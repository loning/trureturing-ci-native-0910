**反例 JT-CE5（覆盖满射不等于全部环境世界重建）。** 取
$S=\{s,z\}$、$U=\{s\}$，两个值域均为 $\{0,1\}$，单个局部关系
$\Gamma_1=\{0,1\}$。按坐标序 $(s,z)$，$J^S$ 含四个世界。令环境约束为 $z=s$，则
$\Gamma=\{(0,0),(1,1)\}$ 只剩两个世界，但
$\pi_U^S(\Gamma)=J=\{0,1\}$。任何只依赖 $s$ 的响应像都保持，而另两个环境延拓已经删除，故 $\Gamma\ne J^S$。

<a id="jt-hidden-constraints"></a>

