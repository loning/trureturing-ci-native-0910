# 三、一个完整修复：让历史可以停止，同时准确保留原始算术权重

现在固定一个有限整数：

$$
B=\prod_{i=1}^{k}p_i^{a_i}.
$$

状态空间是它的因数盒子：

$$
\mathcal X_B
=
\{b:0\le b_i\le a_i\}.
$$

在每个状态，可以继续乘一个尚未达到上限的素数，也可以停止。

停止时输出当前整数：

$$
D=n(b).
$$

目标终点分布规定为：

$$
\boxed{
\Pr(D=d)
=
\frac{d^{-s}}{Z_B(s)},
\qquad
Z_B(s)=\sum_{d\mid B}d^{-s}.
}
\tag{9}
$$

这保留了原来的有限算术配分函数。

