# 二、为什么会冲突？因为“按状态计数”和“按历史计数”不是同一种测度

一个状态 \(b\) 的算术权重是：

$$
n(b)^{-s}.
$$

但它有：

$$
M(b)
$$

条历史。

如果每条历史都直接赋予 \(n(b)^{-s}\)，最终状态就获得：

$$
M(b)n(b)^{-s},
$$

而不是原来的权重。

因此，要保留算术分布，同时给同终点的历史等权，**每条历史必须分到：**

$$
\boxed{
w(b)=\frac{n(b)^{-s}}{M(b)}.
}
\tag{7}
$$

对5040：

$$
M(4,2,1,1)=840.
$$

所以每条到达5040的历史，不应再完整领取一次 \(5040^{-s}\)，而应领取：

$$
\frac{5040^{-s}}{840}.
$$

若把权重写成指数形式：

$$
w(b)=e^{-\mathcal S(b)},
$$

则：

$$
\boxed{
\mathcal S(b)
=
s\ln n(b)+\ln M(b).
}
\tag{8}
$$

第二项是一个**历史重数校正**：求和时产生的 \(M(b)\) 倍历史数量，被它恰好抵消。

这里的 \(\mathcal S\) 是数学上的负对数权重，不预先认定为物理作用量。但它说明：

> **把状态扩展成全历史以后，只保留原来的“能量项”通常不够，还需要处理历史的组合熵。**

这与生成流网络研究的一个核心问题一致：许多不同构造路径可以通向同一个对象，若目标是按对象权重采样，就必须使路径流与终点权重相容。([机器学习研究杂志][2])

