# 十一、对“离散临界几何”的理解，现在可以再精确一层

本轮出现了三类不同、但通过公式相连的边界。

**第一类是余数几何的边界：**

$$
\sum_p(a_p\bmod d)=d.
$$

各个局部寄存器都没有问题，但剩余总量越过一整周期，指定历史相位开始完全相消。

**第二类是解析背景的边界：**

$$
\Re s=\frac1d.
$$

在 \(d\) 次幂核心上求和，会自然出现这一收敛尺度。它不是自动的新无零线；未配掉的素数项仍携带真实解析奇点。

**第三类是全局运输的离散不变量：**

$$
\nu_{\mathcal C}\in\mathbb N.
$$

它不由任何单点读数决定，而由完整闭路上的解析延拓决定。

因此：

$$
\boxed{
\text{局部相消}
\neq
\text{解析奇点消失}
\neq
\text{全局绕行数为零}.
}
$$

这也给 Zeckendorf 编码一项明确任务：它可以规范地编码 \(a_p\)，但若要保留本轮的周期筛选，必须运输真实的：

$$
a_p=db_p+r_p
$$

及其历史权重，不能直接把黄金字串截成长度 \(d\) 的小段，冒充除以 \(d\) 的商与余数。

---

