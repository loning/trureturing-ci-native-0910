# 二十五、绕行可以写成一个局部平坦、整体却不平凡的连接

固定 \(d\ge3\)，令：

$$
r=d-1,\qquad \ell(s)=\log\zeta(s).
$$

在不含零点和极点的局部区域，定义向量：

$$
\boxed{
Y_d(s)=
\begin{pmatrix}
1\\
\ell(s)\\
\ell(s)^2/2!\\
\vdots\\
\ell(s)^r/r!
\end{pmatrix}.
}
\tag{59}
$$

令 \(J_d\) 是下次对角线为一、其余位置为零的矩阵：

$$
J_de_j=e_{j+1}\quad(j<r),
\qquad J_de_r=0.
$$

于是：

$$
J_d^d=0,
$$

并且：

$$
\boxed{
dY_d
=
J_d\frac{\zeta'(s)}{\zeta(s)}\,ds\,Y_d.
}
\tag{60}
$$

前文的每个 \(\mathcal H_d\)，都是这个向量的一项具有全纯系数的线性读出。

在避开零点与极点的区域内，这个连接局部是：

$$
J_d\,d\ell.
$$

因此它的局部曲率为零。

但若闭路 \(\mathcal C\) 围住 \(\nu_{\mathcal C}\) 个零点、没有围住极点，则：

$$
\ell\longmapsto\ell+2\pi i\nu_{\mathcal C}.
$$

这是辐角原理与对数解析延拓的标准关系。([DLMF][1])

对应的整体运输矩阵为：

$$
\boxed{
U_{\mathcal C}
=
\exp\!\left(2\pi i\nu_{\mathcal C}J_d\right).
}
\tag{61}
$$

所以：

$$
\boxed{
\text{局部连接严格平坦}
\quad\not\Rightarrow\quad
\text{沿闭路运输后回到同一个分支}.
}
$$

这里的几何是**穿孔复参数域上的解析连接**，不是已经建立的物理时空度量。

这也给前面“平坦是否只是近似”的讨论补上一种不能遗漏的情况：

> 局部可以真正平坦，整体仍然具有非平凡绕行。问题不一定藏在局部曲率里，也可能藏在局部区域之间的粘合与分支记录里。

---

