### 为什么有下界？

解析性给出局部一致估计

$$
\|f_{a_c+\delta}-f_*\|_\infty\le K\delta.
$$

Toeplitz 二次型是乘法算子的压缩，因此

$$
\|T_N(a_c+\delta)-T_N(a_c)\|_{\mathrm{op}}
\le K\delta.
$$

结合式（14），只要

$$
K\delta<c(N+1)^{-2m_*},
$$

有限矩阵就仍然正定。

所以它不可能过早发现越界。

