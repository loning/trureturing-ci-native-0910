# 十、Zeckendorf与项目工具，现在应该怎样接入？

这一轮可以把四原语落实为一个完整模型：

$$
\boxed{
\begin{aligned}
\mathrm{ADMIT}:&\quad0\le b\le a,\text{以及允许的增层边};\\
\mathrm{FLOW}:&\quad\text{式（11）—（12）的归一化转移};\\
\mathrm{ANCHOR}:&\quad\text{初态、指数上限与终点权重};\\
\mathrm{CUT}:&\quad\text{时间层、能量下闭集或其他因果切面}.
\end{aligned}
}
$$

Zeckendorf负责规范表示每个指数和区块索引。它不会改变：

$$
M(b),\qquad Z_B(s),\qquad H(b),
$$

这些对象的数学含义。

如果改变编码导致它们变化，就必须证明改变的是哪种语义，而不能将其默认为无损压缩。

本轮读取的仓库快照 `f94eef6` 中，有三类可直接衔接的部件：

