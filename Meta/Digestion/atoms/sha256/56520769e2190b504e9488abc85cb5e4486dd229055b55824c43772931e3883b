# 二十四、重新放大被压缩的算术信息：素数幂边界层

定义两类过程的单位时间跳跃强度：

$$
\boxed{
\nu_\varepsilon
=
\sum_{p,k\ge1}
\frac{p^{-k(1+\varepsilon)}}k
\,\delta_{\varepsilon k\ln p},
\qquad
\nu_0(dx)=\frac{e^{-x}}x\,dx.
}
\tag{70}
$$

前者属于原算术过程，后者属于 Gamma 过程。这里的 \(\delta_x\) 表示位于 \(x\) 的点质量。

记跳跃尾函数：

$$
\boxed{
\bar\nu_\varepsilon(x)
=
\nu_\varepsilon((x,\infty)),
\qquad
\bar\nu_0(x)
=
E_1(x)
=
\int_x^\infty\frac{e^{-v}}v\,dv.
}
\tag{71}
$$

它们表示：**每单位时间，大小超过 \(x\) 的事件平均发生多少次。**

每个固定素数幂的位置是：

$$
x=\varepsilon\ln(p^k).
$$

当 \(\varepsilon\downarrow0\) 时，这个位置移向零。因此，为了看清固定算术结构，应当改用：

$$
u=\frac{x}{\varepsilon}.
$$

于是：

$$
\boxed{
\begin{aligned}
D_\varepsilon(u)
&:=
\bar\nu_\varepsilon(\varepsilon u)-E_1(\varepsilon u)\\
&=
\ln\zeta(1+\varepsilon)
-\sum_{p^k\le e^u}\frac{p^{-k(1+\varepsilon)}}k
-E_1(\varepsilon u).
\end{aligned}
}
\tag{72}
$$

右边只需要一个 ζ 值和有限素数幂求和，不需要直接计算无限尾部。

由标准展开：

$$
\ln\zeta(1+\varepsilon)
=
-\ln\varepsilon+O(\varepsilon),
$$

$$
E_1(x)=-\gamma-\ln x+O(x),
$$

得到对每个固定 \(u>0\)：

$$
\boxed{
D_\varepsilon(u)\longrightarrow
B(u):=\gamma+\ln u-M_0(e^u),
}
$$

$$
\boxed{
M_0(X)=\sum_{p^k\le X}\frac1{kp^k}.
}
\tag{73}
$$

所用 ζ Laurent 展开与指数积分级数是经典公式。([DLMF][2])

