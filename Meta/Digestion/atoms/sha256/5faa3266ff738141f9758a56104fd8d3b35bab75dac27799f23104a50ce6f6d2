#### 证明

局部负区域满足

$$
|t|<b_j\delta^{1/(2m_j)}(1+o(1)),
$$

所以其长度直接给出式（36）。

对负部分积分，主项为

$$
\frac1{2\pi}
\int_{-b_j\delta^{1/(2m_j)}}^{b_j\delta^{1/(2m_j)}}
(A_j\delta-B_jt^{2m_j})\,dt.
$$

利用 \(B_jb_j^{2m_j}=A_j\)，积分得到式（37）。缩放后的余项一致趋零，因此不改变主项。证毕。

---

令

$$
m_*=\max_jm_j,
\qquad
\alpha=\frac1{2m_*}.
$$

最平缓的接触点主导两种总量：

$$
\boxed{
d_-(\delta)
=
K_{\mathrm{dim}}\delta^\alpha+o(\delta^\alpha),
}
$$

$$
\boxed{
e_-(\delta)
=
K_{\mathrm{tr}}\delta^{1+\alpha}
+o(\delta^{1+\alpha}),
}
\tag{38}
$$

其中

$$
K_{\mathrm{dim}}
=
\frac1\pi\sum_{m_j=m_*}b_j,
$$

$$
K_{\mathrm{tr}}
=
\frac1\pi
\sum_{m_j=m_*}
\frac{2m_*}{2m_*+1}A_jb_j.
$$

所以：

$$
\boxed{
\text{负区域占比}\asymp\delta^\alpha;
\qquad
\text{负总量}\asymp\delta^{1+\alpha}.
}
\tag{39}
$$

**一个负区域可以包含越来越多可分辨方向，同时每个方向的负深度却很小。**

这正是不能只用“偏离多少”一个数来描述临界的原因。

---

