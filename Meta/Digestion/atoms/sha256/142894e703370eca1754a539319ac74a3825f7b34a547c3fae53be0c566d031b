**命题 65（共同来源算术的实际联合 $\Xi$ 响应）。** 给定定义在同一个非空
$\Gamma$ 上的有限输入族 $H_i:\Gamma\to\mathcal B$，共同来源的指定丰富运算可先取

$$
J_\Xi(\gamma)=(\Xi(H_i(\gamma)))_{i=1}^n,
\qquad
J_\Xi[\Gamma]\subseteq\mathcal D_{\rm ts}^{\,n},
$$

再沿命题 39、41 的精确更新和守卫求值。确切地，对所用基本运算的摘要操作
$f_\Xi:D_{f_\Xi}\subseteq\mathcal D_{\rm ts}^{\,n}\to\mathcal D_{\rm ts}$，
总标签响应与成功世界域为

$$
G_\Xi=\widehat{f_\Xi}\circ J_\Xi,
\qquad
\Gamma_{f_\Xi}=J_\Xi^{-1}[D_{f_\Xi}].
\tag{OD-JOINT-ARITHMETIC}
$$

R8 的实际像、标签结果、冲突及充分域公式全部适用；严格时间复合仍以左摘要的 $M$ 小于右摘要的 $m$ 为准确守卫。

**证明。** 先把全部输入放在同一个 $\Gamma$ 上，故 $J_\Xi[\Gamma]$ 的每个元组都由一个共同合法世界实现。P1 的更新式正是这些合法丰富运算的摘要，且守卫恰好保域；所以逐世界的摘要求值及失败与原运算一致。再按定义 32–33 总化，就直接落入命题 56–61 的类型与结论。对有限复合沿用同一精确更新并严格传播失败即可。若只研究成功后的丰富输出，则须公布 $\Gamma_{f_\Xi}$，并在它非空时才把该限制视为合法不确定历史。不能任取形式五元组，也不能以边缘响应的自由组合替代实际联合像；来源标签既不证明独立性，也不证明共同可实现性或充分性。下面的例子分别展示当前 $q$ 及固定因子非零 $q$ 的不足。证毕。

