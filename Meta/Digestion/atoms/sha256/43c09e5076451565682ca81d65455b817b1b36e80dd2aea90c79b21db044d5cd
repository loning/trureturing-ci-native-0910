# 三十五、有限区域能够认证，但整体还需要一种统一的算术预算

上述方法说明：

对任意固定的、边界不含零点的紧矩形，只要能够持续提高求值精度并取得正确导数界，就存在足够细的有限网格通过证书。

但：

$$
\forall\mathcal R\ \exists\text{有限证书}
$$

不等于：

$$
\exists\text{一个固定证书覆盖全部高度与全部临界线邻域}.
$$

对于 RH，真正缺少的仍然是全局算术控制：

$$
\boxed{
\text{所有位于 }\frac12<\Re s<1
\text{ 的允许闭路，都具有零绕行数}.
}
\tag{83}
$$

已有反射关系把右半临界带的无零性与整个 RH 连接起来，但它没有自行提供式（83）的统一估计。([DLMF][6])

因此，后续有两种不同性质的工作：

**计算性工作**是为指定区域建立有限证书，或者找到某个严格非零计数。

**理论性工作**是从真实 theta、素数或正核结构中，证明一类可以覆盖无界区域的统一约束。

它们可以相互支持，但不能把不断扩大有限扫描范围，直接写成已经完成全局证明。

---

