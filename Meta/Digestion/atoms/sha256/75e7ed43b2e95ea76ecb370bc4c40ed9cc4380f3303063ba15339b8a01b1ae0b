# 八、对实际 ξ，这些结果到底意味着什么？

上一轮定义了实际全阶正性半径

$$
a_*=
\max\{a:T_N(a)\succeq0\text{ 对全部 }N\}.
$$

并得到

$$
\mathrm{RH}\iff a_*=1.
$$

如果 RH 不成立，最近的右侧零点会给出一个单位圆内部的极点半径 \(R_0<1\)。上一轮利用该极点的非实留数证明：

$$
\boxed{
a_*<R_0.
}
\tag{27}
$$

也就是说，假设存在的首次正性失效，会发生在 \(\mathcal F_\xi\) 仍然解析的地方，而不是等到极点出现。

因此，**在这一假设情形下，本轮所有首次接触定理都适用**：

$$
\boxed{
\lambda_{\min}(T_N(a_*))
\asymp N^{-2m_*},
}
$$

$$
\boxed{
a_N^*-a_*
\asymp N^{-2m_*}.
}
\tag{28}
$$

其中 \(m_*\) 是实际接触轮廓的阶数，当前还不知道。

但如果 RH 成立，临界半径是 \(1\)，单位圆上就是零点映像所在的位置；不能再假定 \(\mathcal F_\xi\) 穿过整条单位圆仍然解析。零点与函数方程的标准性质必须在这里保留。([DLMF][5])

所以，本轮的有限偶数阶分类**不能直接套到 RH 成立时的全部单位圆边界**。这也解释了它与前文条件性的

$$
h_N^{1/N}\to0
$$

并不冲突：两者研究的是不同类型的边界。

