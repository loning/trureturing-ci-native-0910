## 定理8.1：首事件逃逸

设 \(X_1^{(m)}\) 表示：

* 立即停止，记为 \(\mathrm{STOP}\)；
* 或第一次乘入的素数。

对任意固定有限素数集合 \(S\)，都有：

$$
\boxed{
\Pr\bigl(X_1^{(m)}\in S\cup\{\mathrm{STOP}\}\bigr)
\longrightarrow0.
}
\tag{27}
$$

### 证明

先从终点分布分析。

写：

$$
D_m=\prod_{p\le m}p^{A_p},
\qquad
L_m=\Omega(D_m)=\sum_{p\le m}A_p.
$$

由于终点权重按素数分解，各 \(A_p\) 相互独立，且服从截断几何分布。

对固定 \(p\)：

$$
\boxed{
\mathbb E[A_p]\le\frac1{p^s-1}.
}
\tag{28}
$$

所以，对固定有限 \(S\)：

$$
\mathbb E\!\left[\sum_{p\in S}A_p\right]\le C_S<\infty,
$$

其中 \(C_S\) 不依赖 \(m\)。

另一方面，只要 \(p\le m\)，其指数上限至少为1，故：

$$
\Pr(A_p\ge1)
\ge
\frac{p^{-s}}{1+p^{-s}}
\ge\frac12p^{-s}.
$$

当 \(0<s\le1\) 时：

$$
\sum_pp^{-s}=\infty.
$$

独立激活指标的数量因此趋于无穷；例如用“方差不超过均值”与 Chebyshev 不等式即可证明：

$$
\boxed{
L_m\longrightarrow\infty
\quad\text{依概率}.
}
\tag{29}
$$

仓库的共同边界模块已经把素数激活和有限支撑性的临界归约到同一个 \(s=1\) 阈值；这里继续分析的是该边界对有序历史的影响。

给定终点计数，历史次序均匀，因此：

$$
\Pr(X_1^{(m)}\in S\mid(A_p))
=
\frac{\sum_{p\in S}A_p}{L_m}
$$

在 \(L_m>0\) 时成立。

对任意整数 \(K>0\)：

$$
\Pr(X_1^{(m)}\in S)
\le
\Pr(L_m<K)+\frac{C_S}{K}.
$$

先令 \(m\to\infty\)，再令 \(K\to\infty\)，得到极限为零。

立即停止的概率则是：

$$
\Pr(X_1^{(m)}=\mathrm{STOP})=\frac1{Z_m(s)}\to0.
$$

证毕。

