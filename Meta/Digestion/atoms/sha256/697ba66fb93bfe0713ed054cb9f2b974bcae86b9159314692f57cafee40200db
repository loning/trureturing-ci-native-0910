# 七、一个必须修正的几何直觉：整体间隙消失，不等于新增高度消失

前文反复使用 Schur 余量

$$
h_N
=
\min_{\deg p<N}
\int
|z^N-p(z)|^2\,d\mu(z).
$$

这是首一正交多项式的平方范数，也可以解释为新增一次读出的预测残差。对应递推关系属于标准单位圆正交多项式理论。([DLMF][2])

但

$$
h_N
$$

不是

$$
\lambda_{\min}(T_N).
$$

在本轮的解析首次接触模型中，二者甚至具有相反的稳定性表现。

