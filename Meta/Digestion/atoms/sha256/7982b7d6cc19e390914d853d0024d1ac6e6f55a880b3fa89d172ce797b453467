## 一个重要的严格性细节

近似多边形 \(\Gamma\) 不是一个全纯函数。

因此，上述证明**不能直接把多边形塞进要求两个函数全纯的 Rouché 定理**。

正确顺序是：

$$
\boxed{
\text{误差界}
\longrightarrow
\text{像曲线零避让同伦}
\longrightarrow
\text{绕数不变}
\longrightarrow
\text{辐角原理}.
}
\tag{73}
$$

若所有 \(w_j\) 都是有理复数，绕数还可以通过有向射线穿越次数，用有理数比较精确计算，不必对每个相位求浮点反正切。

这一类严格复积分与自适应误差控制已有成熟的任意精度算法；本轮提出的是适合当前讨论的一个直接、可审计的充分证书。([arXiv][3])

---

