**证明。** 每个 $u\in J$ 在 $S_i$ 上的限制属于 $\Gamma_i$，所以
$h_i$ 由其全函数类型返回合法丰富状态。在同一 $u$ 上限制，共同变量的值自动相同；$J$ 非空满足定义 15 的共同域前提。在非空 $W\subseteq J$ 上限制这些函数，或先把环境 $\gamma\in\Gamma$ 限制到 $U$，论证完全相同。这一步给出共同来源算术所缺的共同域保证，不把来源赋值的函数并误当成丰富档案的合并。§3 的丰富运算仍执行它们各自的事件标记、旧档案保留与候选事件构造。

一个实际世界成功，恰好是它的实际响应元组属于 $D_f$，因此逆像给出
$W_f$。成功世界的结果显然属于 $f[R_W\cap D_f]$；反之，每个
$y\in R_W\cap D_f$ 都有某个 $u\in W$ 满足 $H(u)=y$，该世界成功且产生
$f(y)$，所以成功结果像恰等于所写直接像。

存在失败世界，恰好是存在已达响应 $y\in R_W\setminus D_f$。成功与失败使用不交标签，因此得到带标签像公式；没有这样的已达失败响应，等价于
$R_W\subseteq D_f$，也就是全域有定义。这同时给出失败存在性、世界逆像与响应直接像之间的完整对应。

非空成功限制模型存在，当且仅当 $R_W\cap D_f\ne\varnothing$。若此交集为空而 $W\ne\varnothing$，所有世界都失败，带标签像为 $\{\bot\}$；若
$W=\varnothing$，带标签像为空集。它们都不是标量成功零的像
$\{\operatorname{ok}(0)\}$。$\square$

上述像公式复用 §34 定义 31–33、命题 58 的严格联合响应语义；连接树在这里负责产生或约束可供该旧消费者使用的世界，未重定义消费者。

### 42.1 固定有限算术表达式及所有内部守卫

对旧整数、有理数或实数接口中的固定有限表达式 $E$，分别取
$A=\mathbb Z,\mathbb Q,\mathbb R$。设标量输入个数为 $m$，把原表达式树的普通部分求值记为

$$
e:D_e\subseteq A^m\longrightarrow A.
$$

$D_e$ 必须包含每个内部运算的原守卫，按原表达式树定义，不能先代数消去再重新赋域。给定实际世界域 $W$，令
$p:W\to A^m$ 为各相应标量输入读数构成的同世界元组，实际标量联合像为
$P_W=p[W]$。则成功数值可能性恰为

$$
e[P_W\cap D_e],
\tag{JT-scalar-success}
$$

而完整严格数值带标签像为所有
$\operatorname{ok}(e(a))$（$a\in P_W\cap D_e$），再且仅在
$P_W\setminus D_e\ne\varnothing$ 时加入 $\bot$。

