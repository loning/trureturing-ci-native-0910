### 证明

令 \(q=|\nu-\mu|\)，取最接近 \(q\alpha\) 的整数 \(m\)。

\(\alpha\) 的代数共轭为 \(-\varphi\)，所以：

$$
(q\alpha-m)(-q\varphi-m)
=
m^2+mq-q^2
$$

是非零整数，其绝对值至少为一。

又因为：

$$
|m-q\alpha|\le\frac12,
$$

得到：

$$
\|q\alpha\|_{\mathbb R/\mathbb Z}
\ge
\frac1{\sqrt5q+\frac12}.
$$

再使用 \(0\le x\le1/2\) 上：

$$
2\sin(\pi x)\ge4x,
$$

即可。证毕。

因此，黄金相位提供的是：

$$
\boxed{
\text{给定范围 }B
\Longrightarrow
\text{明确的 }O(B^{-1})\text{ 分辨率尺度}.
}
$$

它没有提供一个与 \(B\) 无关的固定精度。

这与 Zeckendorf 唯一编码也应区分：**唯一表示解决的是“是否混淆”，稳健测量还要解决“彼此距离有多小”。**

---

