## 十一、从“第一个负方向”，推进到“负方向怎样成批出现”

上一轮已经得到：若首次接触发生在解析区域内，局部轮廓具有

$$
f_{a_c+\delta}(\theta_j+t)
=
-A_j\delta+B_jt^{2m_j}
+\text{高阶项},
\qquad A_j,B_j>0.
$$

因此，越界后的负区域宽度约为 \(\delta^{1/(2m_j)}\)。

现在继续问两个不同的问题：

**第一，长度为 \(N+1\) 的离散历史中，会出现多少个彼此独立的负方向？**

**第二，要把这些负方向全部抬回非负区域，最少需要多大、多少维的正修正？**

这两个量可以同时算出，而且具有不同的临界指数。

沿用

$$
f_a(\theta)=\Re\mathcal F(ae^{i\theta}),
\qquad
T_N(a)=\left(a^{|i-j|}r_{i-j}\right)_{0\le i,j\le N}.
$$

记其本征值为

$$
\lambda_{N,1}(a),\ldots,\lambda_{N,N+1}(a).
$$

定义

$$
\boxed{
\nu_N(a)=\#\{j:\lambda_{N,j}(a)<0\},
\qquad
\mathcal E_N^-(a)
=
\sum_j(-\lambda_{N,j}(a))_+.
}
\tag{33}
$$

这里：

$$
\nu_N:\text{负子空间的维数};
$$

$$
\mathcal E_N^-:\text{全部负本征值的绝对值总量}.
$$

它们不是概率，也不是已经赋予物理单位的能量。以下先研究这两个明确的矩阵量；接回实际 ξ 时，再保留相应前件。

---

