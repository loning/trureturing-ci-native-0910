## 定理27.1：存在一个共同耦合，控制整个时间窗口

可以在同一个概率空间上构造具有正确各自定律的：

$$
Y_\varepsilon,\qquad\mathcal G,
$$

使对任意 \(T>0\)：

$$
\boxed{
\mathbb E
\sup_{0\le t\le T}
|Y_\varepsilon(t)-\mathcal G_t|
\le
T\mathfrak d_\varepsilon
\le
T\varepsilon C_{\mathrm{ar}}.
}
\tag{84}
$$

这里的“共同耦合”是概率比较方法，不是声称两个模型具有同一个物理时钟。

### 证明

对任意正跳跃测度 \(\nu\)，定义尾函数的下降广义逆：

$$
q_\nu(r)
=
\sup\{x>0:\bar\nu(x)>r\},
\qquad\sup\varnothing=0.
$$

由面积分解：

$$
\int_0^\infty
|q_{\nu_\varepsilon}(r)-q_{\nu_0}(r)|\,dr
=
\int_0^\infty
|\bar\nu_\varepsilon(x)-\bar\nu_0(x)|\,dx.
$$

在同一个强度为 \(dt\,dr\) 的 Poisson 随机测度 \(\Pi\) 上，定义：

$$
\boxed{
\begin{aligned}
Y_\varepsilon(t)
&=
\int_{(0,t]\times(0,\infty)}
q_{\nu_\varepsilon}(r)\,\Pi(ds,dr),\\
\mathcal G_t
&=
\int_{(0,t]\times(0,\infty)}
q_{\nu_0}(r)\,\Pi(ds,dr).
\end{aligned}
}
\tag{85}
$$

广义逆把 \(dr\) 推送为对应跳跃强度，因此两者具有正确的过程定律。

又因为两类跳跃都具有有限的一阶总量，这些非负积分在有限时间内几乎必然有限。

逐样本使用：

$$
\sup_{t\le T}|Y_\varepsilon(t)-\mathcal G_t|
\le
\int_{(0,T]\times(0,\infty)}
|q_{\nu_\varepsilon}(r)-q_{\nu_0}(r)|
\,\Pi(ds,dr).
$$

取期望，再用前面的面积恒等式，得到式（84）。证毕。

通过跳跃测度耦合来控制过程路径，有成熟的研究背景；这里针对当前算术过程给出了具体的一阶估计及其边界函数。([arXiv][4])

**现在我们真正跨过了上一轮的缺口：不仅有限几个时间读数接近，整段时间的最大偏离也有统一期望上界。**

但这不是所有路径上的确定性界。它允许小概率出现较大误差，也可能重新配对不同标签的跳跃。

