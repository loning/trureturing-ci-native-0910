# 二十二、真正需要守住的边界：背景解析到 \(1/d\)，不代表零点问题自动推进到 \(1/d\)

周期增大以后，核心项包含：

$$
m^{-ds}.
$$

因此某些背景级数确实可以进入：

$$
\Re s>\frac1d.
$$

但从式（31）可以看出，剩余结构仍包含：

$$
P(2s),P(3s),\ldots.
$$

当跨过 \(\Re s=1/2\) 时，\(P(2s)\) 就不再处于它的普通绝对收敛域；继续往左还会遇到更多缩放后的解析障碍。

所以：

$$
\boxed{
\text{核心求和收敛域变大}
\quad\not\Rightarrow\quad
\text{全部剩余分支都变成全纯背景}.
}
$$

本轮统一使用 \(\Re s>1/2\)，是因为在这个区域内：

$$
j\Re s>1,\qquad j\ge2,
$$

能无条件控制全部缩放素数项。

这项选择没有预先假设 RH；恰恰相反，它把全部尚待研究的零点分支集中到了：

$$
\log\zeta(s).
$$

因此，继续升高 \(d\) 不会自动把 RH 的困难逐层“除掉”。它会改变分支的多项式表达，却没有自行证明这些分支不存在。

---

