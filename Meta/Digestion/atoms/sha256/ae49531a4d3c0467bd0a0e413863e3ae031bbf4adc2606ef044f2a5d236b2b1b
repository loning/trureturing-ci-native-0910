**定义 37（JT-D2：局部来源包与覆盖域）。** 模型指标集 $I$ 有限且非空。每个节点 $i\in I$ 给出 scope $S_i\subseteq S$ 及非空关系
$\Gamma_i\subseteq\mathcal A(S_i)$。令

$$
U=\bigcup_{i\in I}S_i.
$$

$U$ 是这些模型实际覆盖的来源域，不默认为全部 $S$。各 scope 可以为空、无限、相互嵌套或重复，各关系和值域也可以无限。只有 §43 的有限表特化另外要求 scope 和行集有限；一般定理不作这些收缩。

