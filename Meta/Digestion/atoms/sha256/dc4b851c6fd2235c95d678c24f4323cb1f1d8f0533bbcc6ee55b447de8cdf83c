# 二十四、先把“继续增加周期”改成“检验哪些信息真的被保留下来”

这一轮需要推进的，不只是更高阶公式，而是**可观察性与认证成本**。

前文已经把周期历史读数写成：

$$
\mathcal H_d(s)
=
\sum_{j=0}^{d-1}A_{d,j}(s)\bigl(\log\zeta(s)\bigr)^j.
$$

现在可以证明三项更具体的结果：

**第一，即使闭路确实围住零点，记录绕行的矩阵也可以具有与单位矩阵完全相同的全部本征值和幂迹。**

**第二，黄金比例相位可以避免精确的周期混叠，却仍会在斐波那契尺度上产生越来越难分辨的近似混叠。**

**第三，有限观察要可靠地恢复绕行数，必须控制采样点之间的整段路径。只提高每个点的计算精度，不足以弥补步骤间隔过大。**

最后一项已经能在 5040 的历史多项式上作精确认证：**同一条矩形边界，四个角点的朴素连线给出绕数零；经过有误差预算的细分，正确绕数是一。**

下面从公式（59）接续。

---

