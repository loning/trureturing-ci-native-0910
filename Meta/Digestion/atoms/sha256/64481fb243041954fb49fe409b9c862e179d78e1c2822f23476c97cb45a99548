# 九、这不意味着所有因果模型都失败，而是某组要求不能同时保留

定理8.1依赖于一个关键前提：

$$
\boxed{
\text{给定全部终点事件，所有合法串行次序等概率。}
}
$$

当终点需要无限多个事件时，试图继续保留这种完全对称的排序规则，会遇到“如何等概率选出第一个”的困难。

可以改变模型来获得其他极限。

例如，先生成无限素数指数序列，再规定按素数大小依次执行所有事件。这样就不再要求同一终点的所有排序等概率；它可能产生一个有确定离散起点、却永远不结束的路径。

或者保留一个无限指数配置，把时间定义成有限素数窗口的增长，而不是从所有事件中均匀选出下一项。

但这些修改分别改变了：

$$
\boxed{
\text{次序律、时钟、终点空间或观察方式。}
}
$$

所以，结论不是“时空不可能存在”，而是：

> **有限模型中的某些自然规则，不能不加修改地跨过无限极限。必须明确哪些结构继续保持，哪些结构需要替换。**

这比仅说“\(\zeta\) 在1处发散”多了一层信息：我们知道了在一种明确的时空实现中，**哪一个具体操作失去了归一化极限——选择第一个离散事件。**

这里的 \(s=1\) 仍然是正权重级数与历史律的边界，**不是** RH 的零点临界线 \(\Re s=1/2\)。

