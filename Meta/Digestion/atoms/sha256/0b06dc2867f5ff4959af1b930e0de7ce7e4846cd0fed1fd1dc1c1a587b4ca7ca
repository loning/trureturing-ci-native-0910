# 三十二、现在可以把二维区域拼起来：零点数成为离散单元上的整数

对一个矩形网格，假设每个单元边界都经过上述认证，并共用一致的边界采样。

对有向边 \(v\to w\)，定义相位增量：

$$
a_{vw}
=
\operatorname{Arg}\frac{f(w)}{f(v)},
$$

其中选择与已认证的边界路径一致的局部分支。

每个单元 \(C\) 的绕行数为：

$$
\boxed{
n_C
=
\frac1{2\pi}
\sum_{e\in\partial C}a_e
\in\mathbb Z.
}
\tag{77}
$$

如果 \(f\) 在整个单元内全纯、没有极点，则：

$$
n_C\ge0,
$$

并等于该单元内部零点总数。

相邻单元共享的边，方向相反，相位增量相互抵消。因此：

$$
\boxed{
n_{\mathrm{outer}}
=
\sum_C n_C.
}
\tag{78}
$$

这是一项真正的局部—整体拼接律。

**整体不要求同时保留每个单元内部的全部函数值；但必须保留有向边数据、误差证书和整数绕行记录。**

---

