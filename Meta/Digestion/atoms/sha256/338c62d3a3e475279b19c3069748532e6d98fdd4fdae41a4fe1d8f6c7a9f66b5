### 系数级数为什么确实收敛？

在任意固定的：

$$
\Re s\ge\sigma_0>\frac12
$$

上，有限商的 \(t^j\) 系数至多按 \(K^j\) 增长。

式（29）的分母：

$$
(dK+1)_R
$$

则具有 \(K^R\) 的增长。取 \(t^R\) 时，分子中的 \(K\) 次数不会超过 \(R\)，所以可以由一个与 \(m\) 无关的常数控制。

最终级数受到：

$$
\sum_{m\ge1}m^{-d\sigma_0}
$$

控制，而 \(d\ge3\) 保证它收敛。

**因此，这不是把一个形式展开直接当成全局解析定理；收敛预算是明确的。**

---

