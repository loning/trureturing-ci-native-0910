**继续推进以后，一个比“给空间增加时间坐标”更强的约束出现了：不同切面的统计，必须能够拼成同一套前后一致的历史。**

这不是自动成立的。本轮可以证明：

$$
\boxed{
\text{每个固定步数上的算术分布都合法，}
\quad
\text{却可能不存在一个过程同时实现全部这些分布。}
}
$$

但也能构造一种修复：

$$
\boxed{
\text{保留原来的倒数权重，保留全部操作次序，}
\quad
\text{再加入一个明确的停止机制。}
}
$$

修复后，5040的60个因数状态、2647条可停止历史，以及终点为5040的840条完整历史，能够进入**同一个归一化、前后一致的离散模型**。

更深入地，当这个模型扩展到全部素数方向，\(s=1\) 的临界不仅表现为总权重发散，还会表现为：

$$
\boxed{
\text{第一个离散事件的概率，逃离任意固定有限素数集合。}
}
$$

下面建立这些结论。这次研究的核心，是**局部切面合法性与全局历史相容性之间的差别**，而不是再增加一种形式上的坐标。

