### 下界：有限时间不能无限集中在零点上

令

$$
q(e^{i\theta})=\sum_{j=0}^Nv_je^{ij\theta},
\qquad
\sum_j|v_j|^2=1.
$$

则

$$
|q(e^{i\theta})|^2\le N+1.
$$

在每个零点附近取宽度约为 \(1/(N+1)\) 的小区间。把总宽度选得足够小，\(q\) 的总平方质量至多有一半落在这些区间内。

而在区间外，实解析零点的局部展开保证

$$
f_*(\theta)\ge c_1(N+1)^{-2m_*}.
$$

因此至少另一半质量贡献

$$
\int f_*|q|^2\frac{d\theta}{2\pi}
\ge
c_2(N+1)^{-2m_*}.
$$

对全部 \(q\) 取最小值，得到下界。

