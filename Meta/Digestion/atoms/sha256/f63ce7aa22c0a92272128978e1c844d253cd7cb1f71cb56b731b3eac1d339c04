## 怎样保留真正的酉实现？

可以引入无界绕行记录：

$$
|k\rangle,\qquad k\in\mathbb Z,
$$

并让：

$$
|k\rangle\mapsto|k+\nu_{\mathcal C}\rangle.
$$

这个双边移位是酉的。

但随后读取：

$$
\log\zeta(s_0)+2\pi i k
$$

是一项随 \(k\) 无界增长的读出，不能再把它当成始终位于单位圆内的概率振幅。

若只保留有限时钟：

$$
k\bmod M,
$$

就必然把相差 \(M\) 圈的记录合并。

**这里的“更高层记忆”不是玄学补项，而是一项可以证明不能无损塞进固定有限周期寄存器的整数信息。**

---

