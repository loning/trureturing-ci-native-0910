### 为什么也有上界？

使用式（15）的局域历史包络。

其临界能量至多为

$$
C(N+1)^{-2m_*}.
$$

另一方面，定理一保证接触点的径向导数严格为负。这个包络越集中，平均径向导数就越接近 \(-A<0\)。

因此，增加

$$
\delta=C'(N+1)^{-2m_*}
$$

且取足够大的固定 \(C'\)，便会使该测试向量的二次型严格为负。

证毕。

---

定义越过真实边界 \(\delta>0\) 后，第一次能检测到负方向的时间阶数：

$$
N_{\mathrm{det}}(\delta)
=
\min\{N:T_N(a_c+\delta)\not\succeq0\}.
$$

由式（17）得到

$$
\boxed{
N_{\mathrm{det}}(\delta)
\asymp
\delta^{-1/(2m_*)}.
}
\tag{18}
$$

这不是仅有一个充分上界，而是当前 Toeplitz 历史测试中的双边尺度律。

因此，正确的联合临界变量是

$$
\boxed{
X_{\mathrm{crit}}
=
\delta(N+1)^{2m_*}.
}
\tag{19}
$$

当它很小时，有限历史仍可能处于“看起来全部合法”的保护带内；当它足够大时，必定能构造负方向。

