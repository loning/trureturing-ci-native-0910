# 十、Zeckendorf与项目工具，现在应该怎样接入？

这一轮可以把四原语落实为一个完整模型：

$$
\boxed{
\begin{aligned}
\mathrm{ADMIT}:&\quad0\le b\le a,\text{以及允许的增层边};\\
\mathrm{FLOW}:&\quad\text{式（11）—（12）的归一化转移};\\
\mathrm{ANCHOR}:&\quad\text{初态、指数上限与终点权重};\\
\mathrm{CUT}:&\quad\text{时间层、能量下闭集或其他因果切面}.
\end{aligned}
}
$$

Zeckendorf负责规范表示每个指数和区块索引。它不会改变：

$$
M(b),\qquad Z_B(s),\qquad H(b),
$$

这些对象的数学含义。

如果改变编码导致它们变化，就必须证明改变的是哪种语义，而不能将其默认为无损压缩。

本轮读取的仓库快照 `f94eef6` 中，有三类可直接衔接的部件：

**`ItineraryCompletion`** 已经处理完整读出与相容有限前缀，并在有限状态前提下证明有限深度稳定。这里还需要另外加入概率的一致性与边缘化条件，不能只从字串相容性推出概率相容性。

**`MarkovDataProcessing`** 提供有限 Markov 接口与信息处理定理。式（10）—（15）可以给它提供一个实际的、每行已经归一化的转移系统。

**`ZetaPrimeProductCommonBoundary`** 已经形式化整数配分函数、素数激活与全局有限支撑的共同边界。本轮新增的纸面推导，是将这个边界进一步运输到**均匀次序历史的第一步分布**。

这三层各自承担不同的证明责任：

$$
\boxed{
\text{路径能拼接}
\;\longrightarrow\;
\text{概率能拼接}
\;\longrightarrow\;
\text{极限中的总质量仍然保留}.
}
$$

不能跳过中间任何一层。

