# 六、“整个波”也可以保持原来的算术配分函数，而不改掉它

设 \(\mathscr H_b\) 为终点 \(b\) 的全部已停止历史，令不同历史正交。

定义未归一化历史态：

$$
\boxed{
|\Omega_{B,s}\rangle
=
\sum_b
\sqrt{\frac{n(b)^{-s}}{M(b)}}
\sum_{h\in\mathscr H_b}|h\rangle.
}
\tag{21}
$$

则：

$$
\boxed{
\|\Omega_{B,s}\|^2
=
\sum_bM(b)\frac{n(b)^{-s}}{M(b)}
=
Z_B(s).
}
\tag{22}
$$

如果先定义算术态：

$$
|\phi_{B,s}\rangle
=
\sum_bn(b)^{-s/2}|b\rangle,
$$

以及：

$$
V|b\rangle
=
\frac1{\sqrt{M(b)}}
\sum_{h\in\mathscr H_b}|h\rangle,
$$

则：

$$
V^\dagger V=I,
\qquad
|\Omega_{B,s}\rangle=V|\phi_{B,s}\rangle.
$$

所以：

$$
\boxed{
\text{状态波}
\longrightarrow
\text{全部历史波}
}
$$

可以做到严格等距，而不是让路径重数偷偷改变范数。

这一步不会自动产生量子优势，也没有建立真实物理酉演化。它证明的是：**在明确保留历史标签、采用正确的重数校正以后，整体波表示可以与原算术模型完全相容。**

将波归一化当然可行，但研究临界预算时，必须继续保留：

$$
Z_B(s)=\|\Omega_{B,s}\|^2.
$$

否则又把真正需要比较的绝对总权重除掉了。

