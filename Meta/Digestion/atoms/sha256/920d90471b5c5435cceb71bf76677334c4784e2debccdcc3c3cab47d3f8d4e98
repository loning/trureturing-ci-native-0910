## 三十七、所以“时空曲率”首先应定义为：两种传播顺序是否相容

基础素数指数格本身其实非常平坦。

定义增加素数指数的平移：

$$
T_p(a)=a+e_p.
$$

那么

$$
\boxed{
T_pT_q=T_qT_p.
}
\tag{123}
$$

所以原始乘法格在这个意义下没有路径依赖：

$$
p(qn)=q(pn).
$$

相应对数能量也满足混合二阶差分为零：

$$
\Delta_p\Delta_q\log n=0.
$$

因此：

$$
\boxed{
\text{素数坐标本身并没有自动产生曲率。}
}
$$

真正的路径依赖来自：

* 条件；
