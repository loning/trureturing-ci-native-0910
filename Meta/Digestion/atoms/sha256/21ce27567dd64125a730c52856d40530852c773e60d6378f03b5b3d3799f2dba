## 4.1 “恰好在第 \(t\) 步结束”不同于“观察第 \(t\) 步”

令 \(T\) 为停止前已经执行的素数乘法次数。

则：

$$
\boxed{
\Pr(D=n(b)\mid T=t)
=
\frac{n(b)^{-s}}
{\sum_{\substack{d\mid B\\\Omega(d)=t}}d^{-s}}.
}
\tag{19}
$$

这正是固定步数上的算术分布。

但它不是：

$$
\Pr(B_t=b).
$$

它还条件化了一个未来事实：

$$
T=t.
$$

所以，第一节中不相容的那组分布，现在可以作为：

$$
\boxed{
\text{不同停止时间条件下的分布，}
}
$$

而不是同一个未条件化过程的普通时间边际。

