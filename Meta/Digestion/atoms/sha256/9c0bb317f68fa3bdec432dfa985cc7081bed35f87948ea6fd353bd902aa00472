#### 第三步：控制靠近零的相位区域

在接触阶数为 \(2m_j\) 的根附近，越界后两个零点处的导数大小为

$$
\asymp\delta^{1-1/(2m_j)}.
$$

将零水平移动 \(O(\delta^2)\)，使负区间长度只改变

$$
O\!\left(\delta^{1+1/(2m_j)}\right).
$$

全部接触点相加，得到式（44）。

对于负总量，有限 Fourier 误差贡献至多为

$$
O((N+1)\delta^2).
$$

所有负本征值的深度都为 \(O(\delta)\)，所以秩 \(O(L)\) 的边界扰动只产生 \(O(\delta L)\) 的负总量误差。均匀采样积分的剩余误差为 \(O(\delta)\)。合并得到式（45）。证毕。

---

因此，如果联合极限满足

$$
\boxed{
\frac{(N+1)\delta^\alpha}{\log(1/\delta)}
\longrightarrow+\infty,
}
\tag{46}
$$

则可以严格得到

$$
\boxed{
\nu_N(a_c+\delta)
\sim
(N+1)K_{\mathrm{dim}}\delta^\alpha,
}
$$

$$
\boxed{
\mathcal E_N^-(a_c+\delta)
\sim
(N+1)K_{\mathrm{tr}}\delta^{1+\alpha}.
}
\tag{47}
$$

这是一条充分条件，其中的对数因子不宣称最优。

