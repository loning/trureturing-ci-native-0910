# 三十、5040怎样嵌回同一个边界函数？

现在不另造一套模型，而是直接把 Robin 余量写进 \(B\)。

对 \(n\ge3\)，令：

$$
X=\ln n,
\qquad
W(n)=\ln\frac{\sigma(n)}n.
$$

则：

$$
\Delta(n)
=
\gamma+\ln\ln X-W(n).
$$

由 \(B\) 的定义：

$$
\boxed{
\Delta(n)
=
B(\ln X)+M_0(X)-W(n),
\qquad X=\ln n.
}
\tag{94}
$$

这是一条精确恒等式。

其中：

$$
B(\ln X)
$$

来自全部素数幂相对平滑尺度的残差；

$$
M_0(X)-W(n)
$$

比较截止于 \(X\) 的素数幂总权重，与具体整数的有限指数构型。

