# 五、任意因果切面上的概率，都服从一个离散守恒定律

现在真正研究“不同切面是否描述同一整体”。

令 \(\mathcal D\subseteq\mathcal X_B\) 是一个下闭集合：

$$
b\in\mathcal D,\ c\le b
\Longrightarrow c\in\mathcal D,
$$

并且包含初态0。

边界由所有：

$$
b\to c=b+e_i,
\qquad
b\in\mathcal D,\ c\notin\mathcal D
$$

组成。

