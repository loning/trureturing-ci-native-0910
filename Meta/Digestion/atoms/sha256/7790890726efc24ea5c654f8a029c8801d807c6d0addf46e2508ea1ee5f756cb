## 三十八、PRIMEGAME 正好展示：底层格子平坦，控制逻辑却产生有效路径依赖

FRACTRAN 的分数更新，在素数指数空间中本质上是整数平移：

$$
a\mapsto a+\Delta_i.
$$

平移本身可交换。

但第 \(i\) 条指令只能在守卫区域

$$
a\ge v_i
$$

内执行，而且必须采用第一个合法指令。

所以实际程序是

$$
F_i(a)=a+\Delta_i
$$

加上一个状态依赖的分支选择。

如果先执行 \(F_i\)，可能让 \(F_j\) 的 guard 变真或变假；反过来也一样。

因此：

$$
\boxed{
\text{底层 translation group 可以是平坦的，
guarded dynamics 却可以非交换。}
}
\tag{127}
$$

这给“因果逻辑是几何结构来源”一个非常具体的实现：

> **曲折不是来自坐标本身，而来自哪些移动在什么状态下被允许。**

---

