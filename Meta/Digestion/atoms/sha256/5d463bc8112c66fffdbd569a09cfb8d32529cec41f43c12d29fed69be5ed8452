# 十三、时间细分现在受一个精确卷积定律控制

定义上升阶乘：

$$
(\tau)_a=\tau(\tau+1)\cdots(\tau+a-1),
\qquad
(\tau)_0=1.
$$

从式（35）提取系数，得到：

$$
\boxed{
d_\tau(n)
:=
\prod_{p^a\parallel n}\frac{(\tau)_a}{a!},
\qquad
K_{\tau,s}(n)
:=
\Pr(N_s(\tau)=n)
=
\frac{d_\tau(n)n^{-s}}{\zeta(s)^\tau}.
}
\tag{37}
$$

\(\tau=0\) 时使用集中于整数1的分布。

注意：

$$
d_1(n)=1,
$$

但一般的 \(\tau\ne1\) 时，终点权重不再只是 \(n^{-s}\)。

因此，我们没有违反前文的相容性障碍：**没有要求每一个普通时间切片都维持同一套算术权重。**

