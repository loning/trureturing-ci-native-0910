### 证明

复值函数的线性插值误差满足：

$$
\left|
f(s_j+u(s_{j+1}-s_j))
-
\bigl[(1-u)f(s_j)+uf(s_{j+1})\bigr]
\right|
\le
\frac{B_jh_j^2}{8}.
$$

它可以由二阶导数的积分余项直接得到。

加入顶点误差后：

$$
|f(\text{边界点})-\Gamma_j(u)|\le\delta_j.
$$

因此，在真实像曲线与近似像线段之间作直线同伦，始终有：

$$
|\Gamma_j(u)+t(f-\Gamma_j(u))|
\ge d_j-\delta_j>0.
$$

整个闭合曲线的同伦不经过原点，所以绕数相同。

最后，辐角原理把真实像曲线的绕数识别为矩形内的零点数，按重数计。证毕。([DLMF][1])

---

