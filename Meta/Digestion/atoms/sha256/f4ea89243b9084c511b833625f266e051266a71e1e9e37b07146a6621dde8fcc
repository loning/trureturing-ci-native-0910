### 证明

前半段完成 \(b\) 层，后半段完成 \(a-b\) 层。

将两段概率相乘，再除以总指数为 \(a\) 的概率。式（37）中的 \(p^{-sa}\) 与共同归一化因子全部抵消，留下式（44）。证毕。

不同素数在给定最终各轴指数后，仍然独立。

现在取：

$$
B=5040,\qquad T=1,\qquad\tau=\frac12.
$$

这个中间时刻，**全部60个因数状态都具有正概率**。

其平均坐标是：

$$
(2,1,\tfrac12,\tfrac12),
$$

与此前固定第4个单素数事件时的平均坐标相同。

但总事件层数不再恒等于4，而是：

$$
\boxed{
\begin{aligned}
\mathbb E[\Omega(N_s(1/2))\mid N_s(1)=5040]&=4,\\
\operatorname{Var}[\Omega(N_s(1/2))\mid N_s(1)=5040]&=\frac{15}{4}.
\end{aligned}
}
\tag{45}
$$

单轴方差为：

$$
\frac{a\tau(T-\tau)(a+T)}{T^2(T+1)}.
$$

代入四个指数后，总方差是：

$$
\frac{20+6+2+2}{8}=\frac{15}{4}.
$$

对比两种切面：

| 固定条件        | 可能状态数 | 总完成层数的方差 |
| ----------- | ----: | -------: |
