## 14.1 同一终点分布，并不确定唯一的历史

取更小的终点：

$$
12=2^2\cdot3.
$$

在前文的均匀单素数历史模型中：

$$
223,\quad232,\quad322
$$

各占 \(1/3\)。

但在当前包事件模型中，把 \(2^k\) 包展开成连续的 \(k\) 个2以后：

$$
\boxed{
\Pr(223)=\frac5{12},
\qquad
\Pr(232)=\frac16,
\qquad
\Pr(322)=\frac5{12}.
}
\tag{43}
$$

因为一个4包与一个3包的两种排列，各占 \(1/4\)；三个单素数包的三种排列，各占 \(1/6\)。

**因此，我们构造的是另一种保持终点算术权重的时间实现，不是原历史模型的无损重命名。**

对于 FRACTRAN 这样的带守卫、优先级程序，更不能任意把若干步合成一个包：中间可能存在零测试、输出或控制状态切换。必须另行证明这些内部事件得到保留。

