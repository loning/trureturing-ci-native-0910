# 结论

这次真正新增的结构是：

$$
\boxed{
\text{首次正性接触的径向速度，不需要猜测：
它由整条边界的 Poisson 通量决定，并严格为负。}
}
$$

$$
\boxed{
\text{角向接触只能取偶数阶；
这个离散阶数决定有限历史的临界指数。}
}
$$

$$
\boxed{
a_N^*-a_*\asymp N^{-2m_*},
\qquad
N_{\mathrm{det}}(\delta)\asymp\delta^{-1/(2m_*)}.
}
$$

同时，我们排除了一个危险的混同：

$$
\boxed{
\lambda_{\min}(T_N)\to0
\quad\not\Rightarrow\quad
h_N\to0.
}
$$

所以，统一空间、时间与不同切面时，不能只保留一个“曲率”或“高度”。至少要区分**局部接触形状、径向运输速度、整体相干软方向、单步新增残差，以及有限时间的分辨能力**。

实际 ξ 的全阶正性仍未闭合。本轮没有证明 \(a_*=1\)，也没有找到实际的有限触界点。但现在，假设存在这样的触界，它不再是一个没有结构的未知：**它必须满足式（29）的微分条件、式（6）的整体通量约束，以及式（28）的有限时间缩放律。**

这使下一项算术工作更集中：不是继续把所有边界都解释成同一种几何，而是用实际 ξ 的系数与导数，排除这套已经被严格约束住的首次接触结构。

[1]: https://arxiv.org/html/2301.05779v2 "https://arxiv.org/html/2301.05779v2"
[2]: https://dlmf.nist.gov/18.33 "https://dlmf.nist.gov/18.33"
[3]: https://dlmf.nist.gov/1.9 "https://dlmf.nist.gov/1.9"
[4]: https://arxiv.org/abs/1005.4073 "https://arxiv.org/abs/1005.4073"
[5]: https://dlmf.nist.gov/25.10 "https://dlmf.nist.gov/25.10"
