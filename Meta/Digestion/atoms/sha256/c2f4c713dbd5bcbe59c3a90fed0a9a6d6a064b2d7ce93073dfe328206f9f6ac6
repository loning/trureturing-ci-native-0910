# 十四、把全部剩余一次组织起来：最高只能出现 \(P(s)^{d-1}\)

这一节给出一般公式，避免每增加一个周期就重新枚举全部整数类型。

固定 \(d\)，定义有限多项式：

$$
f_{d,b}(z)
=
\sum_{r=0}^{d-1}
\frac{(db+1)_r}{[r]_{\omega_d}!}\,z^r.
$$

所有计算只保留次数小于 \(d\) 的项。由式（28）：

$$
\boxed{
\begin{aligned}
\mathcal H_d(s)
=
\sum_{m\ge1}\frac{w_d(m)}{m^{ds}}
\sum_{R=0}^{d-1}
\frac{[R]_{\omega_d}!}{(dK+1)_R}
[t^R]
\prod_p f_{d,b_p}(tp^{-s}).
\end{aligned}
}
\tag{29}
$$

这里 \([t^R]\) 表示取 \(t^R\) 的系数。

该恒等式首先在：

$$
\Re s>1
$$

中成立；此时涉及的素数级数绝对收敛，可以合法重排。

