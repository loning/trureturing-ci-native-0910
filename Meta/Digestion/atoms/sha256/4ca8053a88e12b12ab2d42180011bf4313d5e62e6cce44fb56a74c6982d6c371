# 三十三、实际 ξ 的路径曲率上界，可以从同一 theta 态取得

为了把有限证书接回实际对象，沿用：

$$
M(w)=\frac{\xi(\frac12+w)}{\xi(\frac12)}
=
\int_{\mathbb R}e^{wx}p(x)\,dx,
$$

其中 \(p\) 是前文固定的正偶 theta 密度。归一化 ξ 的这种概率表示不需要 RH。([arXiv][5])

对：

$$
w=b+it,
$$

有：

$$
M''(w)=\int x^2e^{wx}p(x)\,dx.
$$

所以：

$$
|M''(b+it)|
\le
\int x^2e^{bx}p(x)\,dx
=
M''(b).
$$

又由于 \(p\) 偶，\(M''(b)\) 关于 \(b\) 偶对称，并随 \(|b|\) 增大而不减。因此：

$$
\boxed{
|\Re w|\le B
\Longrightarrow
|M''(w)|\le M''(B).
}
\tag{79}
$$

这给出了一个不依赖未知零点的上界，可以放入式（72）。

