# 三十、把采样真正变成证书：必须同时认证“路径弯曲量”和“离零余量”

设 \(f\) 在一个矩形及其邻域全纯。

将边界分成小线段：

$$
s_0\to s_1\to\cdots\to s_M=s_0.
$$

在每个顶点计算一个近似值 \(w_j\)，并认证：

$$
|f(s_j)-w_j|\le\epsilon_j.
$$

对第 \(j\) 条线段，记：

$$
h_j=|s_{j+1}-s_j|,
$$

并给出整段上的二阶导数上界：

$$
|f''(s)|\le B_j.
$$

定义近似像线段：

$$
\Gamma_j(u)=(1-u)w_j+uw_{j+1},
\qquad0\le u\le1,
$$

以及它到原点的最短距离：

$$
d_j=\min_{0\le u\le1}|\Gamma_j(u)|.
$$

