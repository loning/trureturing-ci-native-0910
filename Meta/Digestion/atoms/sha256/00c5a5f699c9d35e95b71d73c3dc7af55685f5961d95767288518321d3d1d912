## 十七、同一个局部差分，换边界切面后会产生不同的零模式

前面的接触阶数还具有一个精确的离散实现。

定义

$$
f_m(\theta)
=
\frac{|1-e^{i\theta}|^{2m}}{\binom{2m}{m}}.
$$

它在 \(\theta=0\) 有 \(2m\) 阶零点。

对有限序列

$$
v=(v_0,\ldots,v_N),
$$

把区间外的值补为零，记 \(\Delta^m v\) 为 \(m\) 阶差分。Parseval 等式给出

$$
\boxed{
v^*T_N(f_m)v
=
\frac1{\binom{2m}{m}}
\sum_j|\Delta^mv_j|^2.
}
\tag{56}
$$

因此，接触阶数对应一个明确的差分约束阶数，而不是只在连续图像中出现的形状。

但**边界条件仍然是整体结构的一部分**。

以 \(m=1\) 为例：

$$
\boxed{
v^*T_N(f_1)v
=
\frac12\left[
|v_0|^2+
\sum_{j=0}^{N-1}|v_{j+1}-v_j|^2+
|v_N|^2
\right].
}
\tag{57}
$$

中间是传播差分；两端还各有一个固定边界项。

若删去两端项，常数向量就成为精确零模式；保留两端项，所有有限矩阵严格正定。

所以：

$$
\boxed{
\text{同一套局部传播规律}
+
\text{不同边界条件}
\longrightarrow
\text{不同的整体零模式}.
}
\tag{58}
$$

这也是 Toeplitz 与循环边界有相同主导谱分布、但有限极端本征值仍可能不同的原因之一。

本轮精确核对了 \(m=1,\ldots,6\) 的差分分解。这里没有改变实际 ξ，只是在解释接触模型的离散实现。

---

