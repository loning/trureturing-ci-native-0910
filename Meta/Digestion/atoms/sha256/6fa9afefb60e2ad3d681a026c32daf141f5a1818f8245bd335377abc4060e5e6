#### 证明

对归一化系数向量 \(v\)，其多项式满足

$$
|q(e^{i\theta})|^2\le N+1,
\qquad
\int|q|^2d\nu=1.
$$

记 \(P_E=\int_E|q|^2d\nu\)，则

$$
P_E\le(N+1)\nu(E).
$$

二次型满足

$$
\begin{aligned}
v^*T_N(a)v
&\ge b(1-P_E)-MP_E\\
&=b-(b+M)P_E.
\end{aligned}
$$

若它为负，则得到式（100）。证毕。

---

这里的 \(E\) 可以取相关圆帽在 \(|z|=a\) 上的交叠区间之并。

对固定 \(a<1\)，只有有限多个离线配对圆帽可能碰到该圆周；高处圆帽越来越贴近单位圆，不会无限多地穿入一个固定内圆。

如果圆帽很窄，那么式（100）给出必要的历史长度。

但不能把它不加条件地简化为

$$
N\gtrsim\gamma^2/\delta.
$$

因为比值

$$
\frac{b}{b+M}
$$

也必须受到控制。靠近极点时，\(M\) 可能很大；实际总和的正背景 \(b\) 也可能很小。

