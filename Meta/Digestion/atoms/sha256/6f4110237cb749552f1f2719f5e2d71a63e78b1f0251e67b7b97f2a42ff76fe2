# 三十四、勘注:第三十三节的价格取的是另一套约定

第三十三节引用价格窗口时,写下的四个数取自 \(\ln(p^{a+1}-1)/(p^{a}-1)\big/\ln p\) 这一约定,而本卷第二章用的边际收益率是

$$
\eta_{p,a}=\frac{1}{\ln p}\,\ln\frac{1-p^{-(a+1)}}{1-p^{-a}} ,
$$

两者相差恰好常数 \(1\):对每个素数 \(p\) 与每个 \(a\ge1\),\(\ln\frac{p^{a+1}-1}{p^{a}-1}\big/\ln p=1+\eta_{p,a}\)。由于是**同一个常数**加在每一项上,窗口是否非空、上下确界在哪个素数取到、以及两窗口是否共端点,三者都不受影响;受影响的只有那四个数字本身。按本卷的 \(\eta\) 约定,它们是

$$
U(m)=0.00566875008\ldots\ (\text{取于 } p=2),\qquad
L(m)=0.00611228265\ldots\ (\text{取于 } p=43),
$$

$$
U(n)=0.00546820833\ldots\ (\text{取于 } p=47),\qquad
L(n)=U(m)=\eta_{2,7} ,
$$

且 \(\eta_{2,7}=\log_2\frac{2^{8}-1}{2^{7}-1}-1\)。共端点这一结论按原样成立,只是端点的值是 \(\eta_{2,7}\) 而非 \(1+\eta_{2,7}\)。

同样地,第三十三节"上确界的有限化"一段里,对不整除 \(k\) 的素数 \(p\),下一层的边际收益率在本卷约定下是 \(\eta_{p,1}=\ln(1+1/p)/\ln p\),不是 \(\ln(p+1)/\ln p\);两者仍差常数 \(1\),故"关于 \(p\) 严格递减"与"尾部由紧随其后的一个素数封顶"两条结论不变。对上面两个见证,\(\eta_{43,1}=0.0061122826\ldots\)、\(\eta_{47,1}=0.0054682083\ldots\)、\(\eta_{53,1}=0.0047079996\ldots\),递减可见。

因此义务 **W-1** 的正确陈述是:对素数 \(2\le p<q\),

$$
\frac{\ln(1+1/q)}{\ln q}<\frac{\ln(1+1/p)}{\ln p} .
$$

它与第三十三节所写的形式逐点相差常数 \(1\),故两者等价;形式化取此式,以便直接作用于本卷与仓内的 \(\eta\)。**W-2** 至 **W-6** 的陈述不含具体价格数值,不受本勘注影响。

---

---

