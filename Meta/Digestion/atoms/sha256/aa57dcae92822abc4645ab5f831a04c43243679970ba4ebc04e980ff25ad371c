# 十三、从三周期继续：任意周期都有同一条“剩余结构—解析分支”对应

上一轮三周期的结果不是孤立现象。现在可以证明：

> **对任意固定 \(d\ge3\)，同一份等权历史的 \(d\) 周期读数，在 \(\Re s>1/2\) 内都能表示成 \(\log\zeta(s)\) 的一个 \(d-1\) 次多项式；最高次系数不仅明确，而且在整个半平面内不会为零。**

但还会得到一个重要限制：

> **对同一个闭路，提高周期后得到的最高阶绕行差分，只是同一个零点数的不同幂。要取得零点的空间位置，不能只提高周期；必须保留低一阶响应随复参数变化的信息。**

下面接续前文公式编号，从（27）开始。

仍保留前文的等权历史，不改变调度概率：

$$
\varepsilon_d(n)
=
\frac{Q_{\mathbf a(n)}(\omega_d)}{Q_{\mathbf a(n)}(1)},
\qquad
\mathcal H_d(s)=\sum_{n\ge1}\frac{\varepsilon_d(n)}{n^s},
$$

其中：

$$
\omega_d=e^{2\pi i/d}.
$$

多重集历史的逆序生成函数仍是经典的 \(q\)-多项式，而不是另外选择的相位模型。([DLMF][1])

将每个指数写成：

$$
a_p=db_p+r_p,
\qquad 0\le r_p<d,
$$

并令：

$$
m=\prod_pp^{b_p},
\qquad
K=\Omega(m),
\qquad
R=\sum_pr_p.
$$

前文已经证明，只有 \(R<d\) 的剩余才能通过周期筛选。

定义：

$$
\boxed{
w_d(m)
=
\frac{K!\prod_p(db_p)!}
{(dK)!\prod_pb_p!}.
}
\tag{27}
$$

它是立方、平方情形的统一形式，并满足：

$$
0<w_d(m)\le1.
$$

使用上升阶乘：

$$
(x)_R=x(x+1)\cdots(x+R-1),
\qquad (x)_0=1,
$$

周期筛选的完整非零权重为：

$$
\boxed{
\varepsilon_d\!\left(m^d\prod_pp^{r_p}\right)
=
w_d(m)
\frac{[R]_{\omega_d}!}{(dK+1)_R}
\prod_p
\frac{(db_p+1)_{r_p}}{[r_p]_{\omega_d}!},
\qquad R<d.
}
\tag{28}
$$

当 \(R\ge d\) 时，读数为零。

这里所有分母中的：

$$
[r_p]_{\omega_d}!,
\qquad r_p<d,
$$

都非零。不能先把整个阶乘商机械代入单位根，再忽略其中的零点约分。

式（28）说明：

