# 一、统一时空的第一条额外约束：切面必须能够边缘化

仍然把一个事件定义为“乘入一个素数”。

固定有限素数集合：

$$
P=\{p_1,\ldots,p_k\}.
$$

执行 \(t\) 个事件后，计数向量为：

$$
b=(b_1,\ldots,b_k),
\qquad |b|:=\sum_i b_i=t.
$$

它对应整数：

$$
n(b)=\prod_i p_i^{b_i}.
$$

到达同一计数 \(b\) 的不同操作字串，共有：

$$
\boxed{
M(b)=\frac{t!}{\prod_i b_i!}.
}
\tag{1}
$$

现在提出一个看似合理的要求：

> 每个时间 \(t\) 上，整数状态按 \(n^{-s}\) 加权；给定终点计数，同样的操作次数按所有次序等概率排列。

令：

$$
q_i=p_i^{-s}>0,
$$

$$
C_t=\sum_{|b|=t}\prod_iq_i^{b_i}.
$$

要求的计数分布是：

$$
\boxed{
\Pr(B_t=b)=\frac{\prod_iq_i^{b_i}}{C_t}.
}
\tag{2}
$$

于是，一条具体字串 \(w\) 的概率必须为：

$$
\boxed{
\Pr(W_{1:t}=w)
=
\frac{\prod_iq_i^{b_i}}{C_tM(b)},
\qquad \operatorname{count}(w)=b.
}
\tag{3}
$$

每一个固定 \(t\) 的分布，都完全归一化。

**但这些分布能不能属于同一个前向过程？**

