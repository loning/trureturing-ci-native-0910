# 一、固定同一份历史，只改变周期读出

沿用前文的生成规则：从 1 出发，每一步乘一个素数。

对：

$$
n=\prod_p p^{a_p},
\qquad
L=\Omega(n)=\sum_p a_p,
$$

历史集合记为 \(\Gamma_{\mathbf a}\)。其中类型 \(p\) 恰好出现 \(a_p\) 次，历史数量为：

$$
h(\mathbf a)=\frac{L!}{\prod_p a_p!}.
$$

定义逆序面积：

$$
A(\gamma)
=
\#\{(i,j):i<j,\ p_i>p_j\}.
$$

它的生成多项式为：

$$
\boxed{
Q_{\mathbf a}(q)
=
\sum_{\gamma\in\Gamma_{\mathbf a}}q^{A(\gamma)}
=
\frac{[L]_q!}{\prod_p[a_p]_q!}.
}
\tag{1}
$$

这是经典的多重集逆序生成恒等式。([DLMF][1])

本轮始终保留**等权历史**，不改变调度概率。上一轮已经证明，若更换历史概率，部分相消零点会移动，因此这一前件不能省略。

