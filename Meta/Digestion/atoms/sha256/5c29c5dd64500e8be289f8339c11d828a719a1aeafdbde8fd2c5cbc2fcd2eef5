## 43. JT：有限表特化的证明推论

在有限非空节点集上，进一步假设 scopes 有限且显式列出，各关系是非空有限表，行是各 scope 上完整的有类型赋值，所表示值的相等可判。值域本身无须有限。对一棵指定树确认 RI，并逐边比较两份有限 separator 投影是否相等，命题 67 就给出非空自然连接及每个原行可延拓；无需构造或枚举全部 $J$，也不增添所有节点对的检查。对有效 RI 树，投影忠实性反过来蕴含每条所检 EC，所以在该有限表范围内这些边等式准确对应于保持全部宣称局部行的条件。

若某条 EC 失败，其有限见证是某侧出现而另侧不出现的完整 separator 赋值。任何在该侧实现它的局部行都不能延拓到全局世界，因为另侧没有匹配的限制。这证明一个宣称局部可能性丢失，**不必然**证明连接为空，反例 JT-CE2 已给出明确区别。RI 失败只说明这棵指定树的证书不可用，不自动说明具体局部关系族不相容；反例 JT-CE3 的成功循环族保留了这一边界。

条件满足之后，任意给定局部行都可作为根元组，依命题 67 的父子构造，在每个子表中取具有所需完整 separator 值的行逐步延拓。每步见证的存在已由 EC 证明。这只是公开延拓证明在有限表上的推论，不另立 witness-search 合同、导出子包 API 或验证服务。

此特化不引入 semijoin reducer 去悄悄修改宣称的 $\Gamma_i$，也不加入循环求解器、运行时 checker、程序或全世界枚举要求。任意无限对象的投影相等、一般 $K$ 的成员关系及一般实数类守卫，不因有限表推论而变得可判。这里没有渐近复杂度、输出规模、最优性或枚举能力的承诺。

<a id="jt-sources"></a>

