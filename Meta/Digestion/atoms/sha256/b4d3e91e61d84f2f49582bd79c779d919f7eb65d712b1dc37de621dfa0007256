# 三十九、A025529 的「合数解只有素数平方」为假,若提升引理成立

本节是**预登记**,写在实施之前。与前几节不同,本节的反例**不能由计算验证**,它依赖一条尚未证明的提升引理;这一点写在最前面,不放在末尾。

**档位与文献核对**。第一档(新近小猜想)。核对对象为 OEIS **A025529** 条目正文,已逐字读取 `https://oeis.org/search?q=id:A025529&fmt=text`。定义(`%N`):

> `a(n) = (1/1 + 1/2 + ... + 1/n)*lcm{1,2,...,n}.`

即 \(a(n)=H_n\cdot L_n\),\(L_n=\operatorname{lcm}(1,\dots,n)\)。被反驳的断言(`%C`,Amiram Eldar 与 Thomas Ordowski,2019-08-07,逐字):

> `It seems that composite numbers n such that n | a(n-1) are only the squares n = p^2 of primes p > 3.`

同一段还记着两条本节要用的事实:`By Wolstenholme's theorem, if p > 3 is a prime, then p^2 | a(p-1).` 与 `Primes p such that p^3 | a(p-1) are the Wolstenholme primes A088164.`

**候选反例**。取 Wolstenholme 素数 \(p=16843\),

$$
n=p^{3}=4\,778\,134\,229\,107 .
$$

它是合数,且不是任何素数的平方(其唯一素因子的指数为 3)。若下述提升引理成立,则 \(n\mid a(n-1)\),该断言为假。

**已验读数(本轮实测)**。

其一,\(p=16843\) 确为 Wolstenholme 素数:因 \(p\nmid L_{p-1}\),有 \(p^{3}\mid a(p-1)\iff H_{p-1}\equiv0\pmod{p^{3}}\);对 \(k=1,\dots,p-1\) 逐项求模逆并求和,得

$$
H_{p-1}\equiv 0 \pmod{p^{3}} .
$$

该计算是 \(p-1=16842\) 步模运算,秒级完成,**不构造 \(L_{p-1}\)**。对照:\(p=17,101,1009\) 处同一求和只在模 \(p^{2}\) 为零(此即 Wolstenholme 定理),模 \(p^{3}\) 分别为 \(2890\)、\(428442\)、\(205652362\)。

其二,**赋值与指数无关**。记 \(v_p\) 为 \(p\)-adic 赋值。实测

| \(p\) | \(v_p(a(p-1))\) | \(v_p(a(p^{2}-1))\) | \(v_p(a(p^{3}-1))\) |
|---|---|---|---|
