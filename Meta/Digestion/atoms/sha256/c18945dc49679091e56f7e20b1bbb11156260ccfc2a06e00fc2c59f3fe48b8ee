## 定理5.1：停止质量与穿越流的精确守恒

$$
\boxed{
\sum_{b\in\mathcal D}\Pr(D=n(b))
+
\sum_{\substack{b\to c\\\text{穿越 }\partial\mathcal D}}
\frac{M(b)H(c)}{Z_B(s)}
=1.
}
\tag{20}
$$

### 证明

到达 \(b\) 而尚未停止的每条前缀，其概率为：

$$
\frac{H(b)}{Z_B(s)}.
$$

这样的前缀共有 \(M(b)\) 条，所以访问 \(b\) 的概率为：

$$
\frac{M(b)H(b)}{Z_B(s)}.
$$

再乘以从 \(b\) 到 \(c\) 的条件概率，得到穿越该边的概率：

$$
\frac{M(b)H(c)}{Z_B(s)}.
$$

每条路径要么在 \(\mathcal D\) 内停止，要么通过唯一的一条边离开它。下闭性和单调增长保证离开以后不可能回来。两类事件互不相交且覆盖全部路径，求和即为1。证毕。

这是一个离散的源—边界流守恒关系。

