**例 G1（相同边缘像和个别依赖不能决定共同来源加法）。** 取唯一来源集 $S=\{s\}$、$V_s=\{0,1\}$，$\Gamma=\{0,1\}$ 表示该来源的两个赋值。固定整数目标 $F(s)=s$；在模型一令第二输入 $B(s)=s$，在模型二令 $B(s)=1-s$。两模型中，每个输入的边缘像都是 $\{0,1\}$，充分域族都只有 $\{s\}$，因而各自的唯一最小充分域相同。然而共同世界上的联合像与加法像分别为

$$
\begin{array}{c|c|c}
& (F,B)[\Gamma] & (F+B)[\Gamma]\\ \hline
\text{模型一}&\{(0,0),(1,1)\}&\{0,2\}\\
\text{模型二}&\{(0,1),(1,0)\}&\{1\}.
\end{array}
$$

第二个加法目标已经恒定，第一个仍依赖 $s$。这里只是将反例中的第二输入改记为 $B$，以区别于定义 33 的组合输出 $G$；联合约束造成的差别不能从两份边缘像及个别依赖族恢复。

