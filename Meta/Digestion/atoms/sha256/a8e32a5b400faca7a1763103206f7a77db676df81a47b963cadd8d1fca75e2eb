## 定理14.1：给定最终指数后，包事件数量有一个精确分布

对每个素数 \(p\)：

$$
\boxed{
\mathbb E\left[
z^{\sum_kC_{p,k}(1)}
\mid A_p(1)=a
\right]
=
\frac{(z)_a}{a!}.
}
\tag{40}
$$

### 证明

同时标记总指数和包数量，联合母函数为：

$$
\exp\left(
\sum_{k\ge1}\frac{q^k}{k}(zx^k-1)
\right)
=
(1-q)(1-qx)^{-z}.
$$

提取 \(x^a\) 系数，得到：

$$
(1-q)q^a\frac{(z)_a}{a!}.
$$

再除以：

$$
\Pr(A_p(1)=a)=(1-q)q^a,
$$

即得。证毕。

对5040，将四个方向相乘：

$$
\boxed{
\mathbb E[z^{C(1)}\mid N_s(1)=5040]
=
\frac{z^4(z+1)^2(z+2)(z+3)}{48}.
}
\tag{41}
$$

于是：

| 包事件数量 |      条件概率 |
| ----: | --------: |
