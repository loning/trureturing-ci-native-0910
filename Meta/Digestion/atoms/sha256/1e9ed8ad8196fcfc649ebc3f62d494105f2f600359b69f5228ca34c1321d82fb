### 定理七：固定越界量下的负方向密度

固定一个充分小的 \(\delta>0\)，则

$$
\boxed{
\lim_{N\to\infty}
\frac{\nu_N(a_c+\delta)}{N+1}
=
d_-(\delta),
}
\tag{40}
$$

并且

$$
\boxed{
\lim_{N\to\infty}
\frac{\mathcal E_N^-(a_c+\delta)}{N+1}
=
e_-(\delta).
}
\tag{41}
$$

第二式使用连续函数 \(x\mapsto(-x)_+\)；第一式则通过连续函数从上下逼近负半轴的示性函数得到。

因此，先让历史长度趋于无穷，再让越界量趋零，有

$$
\boxed{
\frac{\nu_N}{N+1}
\sim K_{\mathrm{dim}}\delta^\alpha,
\qquad
\frac{\mathcal E_N^-}{N+1}
\sim K_{\mathrm{tr}}\delta^{1+\alpha}.
}
\tag{42}
$$

这里必须保留极限顺序。

