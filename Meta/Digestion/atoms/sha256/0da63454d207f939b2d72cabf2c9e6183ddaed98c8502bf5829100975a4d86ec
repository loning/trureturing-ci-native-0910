# 十一、它怎样回到5040与RH，而不把新模型误当作证明？

在 \(s=1\) 时，任意整数 \(B\) 的模型满足：

$$
Z_B(1)=\frac{\sigma(B)}B.
$$

所以：

$$
\boxed{
\Pr_B(\text{立即停止})
=
\frac1{Z_B(1)}
=
\frac{B}{\sigma(B)}.
}
\tag{31}
$$

Robin 判据因而等价于：

$$
\boxed{
\Pr_B(\text{立即停止})
>
\frac{e^{-\gamma}}{\ln\ln B}
\qquad\forall B>5040.
}
\tag{32}
$$

这是对原判据的精确翻译。Robin 与 RH 的等价关系是经典定理。([arXiv][4])

它提供了一个直观但有限的解释：

> **整个后续历史的算术权重不能大到使初始停止概率低于指定阈值。**

然而，式（10）只保证 \(H(0)\) 被正确计算和归一化，并没有证明：

$$
H(0)<e^\gamma\ln\ln B.
$$

正权重、流守恒、规范编码和全部历史的一致性，仍然不能替代这个尖锐的全局上界。

因此，本轮的真正进展不是“概率模型已经证明RH”，而是：

$$
\boxed{
\text{终于能够在不改变原算术权重的前提下，}
\quad
\text{把所有状态、所有次序、停止与不同切面放进同一模型。}
}
$$

此后研究任何几何、熵、波或不变量，都可以明确检查：它是否仍在约束原来的 \(Z_B(1)\)，而不是另一个悄悄改变过权重的对象。

