# 四十、W-1 的逃逸内容重新预登记:该单调性仓内已有,W-1 判为绑定即得

本节是**勘注加重新预登记**。第三十三节末的「预登记的逃逸内容」把 W-1 的非绑定内容写作「一条此前不在仓内的解析单调性引理」;**该前提为假**,本节据实更正,并给出 W-1 的新落地依据。更正由两席独立评审在实施后指出,本节的每一条读数由 orchestrator 复核仓内源码后写下。

**事实一:该单调性此前已在仓内,且已冻结。** `D5/S3/Arith/GoldenResource/GoldenSmallestMissingPrime` 中的

$$
\texttt{golden\_layer\_marginal\_one\_strictAnti}:\quad p,q\ \text{素},\ p<q\ \Longrightarrow\ \rho(q,1)<\rho(p,1)
$$

已是冻结定理,其中 \(\rho\) 即本卷 \(\eta\) 约定下的首层边际。

**事实二:两种写法等价,差常数 \(1\)。** 第三十四节的价格约定勘注已记:

$$
\rho(p,1)=\frac{\ln\!\bigl(1+p^{-1}\bigr)}{\ln p}=\frac{\ln (p+1)}{\ln p}-1 .
$$

故 W-1 所写的 \(\ln(q+1)/\ln q<\ln(p+1)/\ln p\) 与上述冻结定理**逐点相差同一个常数**,二者互为改写。合事实一与事实二:「此前不在仓内」不成立,**原预登记的逃逸内容不存在**。

**判形:W-1 为绑定即得。** 把局部别名与私有引理全部内联后,W-1 的两个半边各自只用到以下操作。

其一,尾单调性半边 = 冻结定理的实例化,加上述常数移位。移位本身由 \(x+1=x\,(1+1/x)\) 与对数乘法法则给出,属定义展开与环规范化。

其二,有限最大值半边 = 冻结的上确界达成规范 `golden_upper_price_spec` 的实例化,加 `Finset.le_max'` 与 `Finset.max'_le` 的应用,加对「是否整除」的分情形与传递性。不整除的素数由冻结的阈值引理 `golden_layer_marginal_one_threshold_of_le` 一步压到最小缺失素数处。

两个半边都点不出一条满足四项条件的新中间命题,故 W-1 **不是** escape-witness。

**新的落地依据:atom-required-bridge。** 三项俱在。

其一,**atom 明文要求**。W-1 的原句两句都要:单调性,以及「它把 \(U(k)\) 由对全体素数的上确界化为有限最大值」。

其二,**此前无类型化的边**。仓内此前没有把 \(U(n)\)(对全体素数取上确界)与一个**有限**集合上的最大值连起来的声明;也没有把本卷的 \(\ln(p+1)/\ln p\) 写法与仓内的 \(\rho(p,1)\) 写法连起来的声明。这两条边是本次新增。

其三,**预登记的具名下游消费者**。第三十三节末已写「W-2、W-3 依赖 W-1」;W-2 与 W-3 是 \(m\)、\(n\) 的极大丰数证书,二者要对**有限**个素数逐一比较边际,正是本条把上确界化为有限最大值之后才可写。

**覆盖归属的更正**。W-1 的第二句是**无条件**陈述:\(U(k)\) 化为有限最大值。故承载它的定理必须自己给出最小缺失素数的存在性,而不是把该存在性留在假设位——存在性已由同目录冻结的 `exists_smallest_missing_prime_threshold` 提供,两者复合不需要新数学。带假设的推论只是辅助形式,不得作为 W-1 第二句的覆盖属主。

**不受影响者**。W-2 至 W-6 的陈述与依赖关系不变;第三十三节的停止判据不变。W-5 仍为该节预登记的另一处非绑定内容,本节不改动它。

---

---

