### 上界：构造一个恰好适配接触阶数的历史包络

选取阶数为 \(2m_*\) 的零点 \(\theta_0\)。

定义

$$
D_L(t)=\sum_{j=0}^{L-1}e^{ijt},
$$

并使用归一化多项式

$$
\boxed{
q_L(e^{i\theta})
\propto
D_L(\theta-\theta_0)^{m_*+1}.
}
\tag{15}
$$

它的次数为

$$
(m_*+1)(L-1).
$$

选 \(L\asymp N\)，即可使其次数不超过 \(N\)。

利用

$$
|D_L(t)|\le\min\{L,C/|t|\},
$$

以及接触点附近

$$
f_*(\theta_0+t)\le C'|t|^{2m_*},
$$

可得

$$
\int |D_L|^{2m_*+2}\,d\theta
\asymp L^{2m_*+1},
$$

而加权分子满足

$$
\int f_*|D_L|^{2m_*+2}\,d\theta=O(L).
$$

两者相除，得到

$$
\int f_*|q_L|^2\frac{d\theta}{2\pi}
=O(L^{-2m_*})
=O(N^{-2m_*}).
$$

证毕。

---

这里调整的是**测试历史的包络**，不是对实际 ξ 系数加平滑、删尾项或重新选一个更容易为正的模型。

这是多切面分析中比较安全的一种操作：

$$
\boxed{
\text{保持实际二次型不变，只设计更适合读取其边界的输入。}
}
\tag{16}
$$

