**对于同一闭路，所有这些最高阶差分，归一化后只是同一个整数的不同幂。**

这不意味着完整的 \(\mathcal H_d\) 没有其他信息；它意味着：

> 不能仅因得到 \(\nu^2,\nu^3,\nu^4\)，就宣布发现了三种独立的零点结构。

同样，绕两个闭路的先后次序，只会把：

$$
\ell\mapsto\ell+2\pi i\nu_1+2\pi i\nu_2.
$$

所以这族最高阶读数并不读取闭路次序的非交换信息。

若要定位零点，必须换一种读出，而不只是继续升高周期。

---

