**判形:W-1 为绑定即得。** 把局部别名与私有引理全部内联后,W-1 的两个半边各自只用到以下操作。

其一,尾单调性半边 = 冻结定理的实例化,加上述常数移位。移位本身由 \(x+1=x\,(1+1/x)\) 与对数乘法法则给出,属定义展开与环规范化。

其二,有限最大值半边 = 冻结的上确界达成规范 `golden_upper_price_spec` 的实例化,加 `Finset.le_max'` 与 `Finset.max'_le` 的应用,加对「是否整除」的分情形与传递性。不整除的素数由冻结的阈值引理 `golden_layer_marginal_one_threshold_of_le` 一步压到最小缺失素数处。

两个半边都点不出一条满足四项条件的新中间命题,故 W-1 **不是** escape-witness。

