**命题 58（全域成功与带标签的精确结果像）。** 原 $f\circ J$ 在整个 $\Gamma$ 上有定义，当且仅当

$$
J[\Gamma]\subseteq D_f
\quad\Longleftrightarrow\quad
\Gamma_f=\Gamma.
\tag{JR-TOTAL-SUCCESS}
$$

原非空共同域上的全部标签结果恰为

$$
G[\Gamma]=\widehat f[J[\Gamma]]
=
\{\operatorname{succ}(y):y\in f[J[\Gamma]\cap D_f]\}
\ \cup\
\begin{cases}
\{\bot\},&J[\Gamma]\setminus D_f\ne\varnothing,\\
\varnothing,&J[\Gamma]\setminus D_f=\varnothing.
\end{cases}
\tag{JR-RESULT-IMAGE}
$$

去掉成功标签后的成功可能值恰为 $f[J[\Gamma]\cap D_f]$，而

$$
\bot\in G[\Gamma]
\quad\Longleftrightarrow\quad
J[\Gamma]\setminus D_f\ne\varnothing.
\tag{JR-POSSIBLE-FAILURE}
$$

**证明。** 部分函数在全域存在，逐世界恰等价于 $J(\gamma)\in D_f$，也就等价于实际像包含于定义域。结果集合是共同世界图像经 $\widehat f$ 的直接像；把实际联合像分成守卫成立和不成立两部分，分别得到成功标签集合与所写失败标签条件。标签的非交性保证普通 $0$、普通常数与失败不被同一元素混淆。若只报告限制到 $\Gamma_f$ 的函数，必须另外公布该域及其是否非空。特别地，$\Gamma_f=\varnothing$ 时成功可能值为空，原非空 $\Gamma$ 上仍有合法总标签响应 $G\equiv\bot$；但空成功域不能作为定义 15 意义下的合法非空不确定历史。证毕。

