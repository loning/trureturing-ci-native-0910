#### 证明

若 \(\operatorname{rank}R<\nu_-(K)\)，则 \(R\) 的零空间必与 \(K\) 的负子空间有非零交点。

取该交点中的 \(x\ne0\)，便有

$$
x^*(K+R)x=x^*Kx<0,
$$

矛盾。于是得到秩下界。

再对 \(K\) 的负本征向量逐一使用 \(K+R\succeq0\)：

$$
\langle v_j,Rv_j\rangle\ge-\lambda_j.
$$

相加，并利用 \(R\succeq0\)，得到

$$
\operatorname{Tr}R\ge\sum_{\lambda_j<0}(-\lambda_j).
$$

而 \(R=K_-\) 同时达到两种下界。证毕。

---

把它用于 \(T_N(a_c+\delta)\)，在式（46）的联合区域内：

$$
\boxed{
\text{最少正修正维数}
\sim
(N+1)K_{\mathrm{dim}}\delta^\alpha;
}
$$

$$
\boxed{
\text{最少正修正迹}
\sim
(N+1)K_{\mathrm{tr}}\delta^{1+\alpha}.
}
\tag{50}
$$

所以，靠近边界时可以出现：

$$
\boxed{
\text{需要修正的方向很多，但每个方向只需要很小修正。}
}
\tag{51}
$$

这比“补一根柱子”“再升一维”更精确。

但这里必须保留一个原则：**添加 \(R\) 会改变原矩阵的实际读数。它是衡量缺陷规模的诊断，不是原对象正性的证明。**

如果只是把原矩阵原封不动地保留为一个更大 Gram 矩阵的主子矩阵，那么无论增加多少维，都不能修复它已有的负方向。

