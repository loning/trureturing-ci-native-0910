# 二十三、项目中的下一份完整对象，应同时保存“次数、位置与分支记录”

本轮核对了快照 `b9e3887853…` 中的有限时钟和 Rouché 零点稳定性相关内容。

`WindowRegister` 已经承担有限周期寄存器的酉结构；`RoucheZeroCount` 已经承担严格边界扰动下、带重数的零点数稳定性。后者并未自动提供某个具体矩形上的边界余量，也没有把任意高阶分支求值当成已经认证的操作。

这次能够补上的理论链是：

$$
\boxed{
\begin{aligned}
\text{真实指数的 }d\text{ 周期余数筛选}
&\longrightarrow
\mathcal H_d\\
&\longrightarrow
\sum_{j=0}^{d-1}A_{d,j}(\log\zeta)^j\\
&\longrightarrow
\text{最高阶差分计数}\\
&\longrightarrow
\text{低一阶差分＋微分定位}\\
&\longrightarrow
\text{带误差的有限零点多项式}.
\end{aligned}
}
\tag{58}
$$

每个箭头都必须保留同一个函数芽和相同的历史权重。

黄金编码在这里继续负责无损表示指数与商余数；它不能把：

$$
a_p=db_p+r_p
$$

随意替换成“把黄金字串切成长度 \(d\) 的块”。根单位筛选使用的是整数除法，不是字符串切片。

本轮辅助核对包括：

* 990 组指数配置—周期组合的精确分圆余式检验；
