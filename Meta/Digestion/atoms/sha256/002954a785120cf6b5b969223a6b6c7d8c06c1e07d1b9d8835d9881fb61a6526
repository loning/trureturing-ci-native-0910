## 24.1 这个边界函数逐个记录全部素数幂

在不包含素数幂事件的区间：

$$
B'(u)=\frac1u.
$$

而在：

$$
u=\ln(p^k)
$$

处，出现跳跃：

$$
\boxed{
B(u+)-B(u-)=-\frac1{kp^k}.
}
\tag{74}
$$

因此，\(B\) 不是任意的误差函数。它记录了：

$$
\boxed{
\text{每个素数幂出现在哪里，}
\quad
\text{以及它贡献多少跳跃权重。}
}
$$

先在固定宏观位置 \(x\) 取极限，会看不见每个固定的小素数。

先把位置放大为 \(u=x/\varepsilon\)，再取极限，则重新得到定义在全部 \(u>0\) 上的算术清单。

这里的 \(x=O(\varepsilon)\) 只针对每个固定素数幂，不是对全部无界素数幂的一致估计。

**5040本身不是素数幂，所以它不是 \(B\) 的一个跳点。它的特殊性仍然需要由多个算术方向共同构成，而不是把它当成单个基本事件。**

