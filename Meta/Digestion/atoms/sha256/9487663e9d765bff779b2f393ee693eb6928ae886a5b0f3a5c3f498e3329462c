## 定理1.1：算术切面与均匀次序的相容性障碍

对 \(k\ge2\)，如果式（2）—（3）在全部时间上来自同一个过程，那么必须：

$$
\boxed{
q_1=q_2=\cdots=q_k.
}
\tag{4}
$$

反过来，所有 \(q_i\) 相等时，确实存在这样的过程。

### 证明

同一个过程必须满足前缀一致性：

$$
\Pr(w)=\sum_i\Pr(wi).
$$

因为：

$$
M(b+e_i)=M(b)\frac{t+1}{b_i+1},
$$

代入式（3），得到必要条件：

$$
\boxed{
\frac{C_{t+1}}{C_t}
=
\frac1{t+1}
\sum_iq_i(b_i+1)
\qquad\forall b,\ |b|=t.
}
\tag{5}
$$

左边只依赖时间，不能依赖当前计数。

取 \(t=1\)，分别令 \(b=e_j\)，则：

$$
2\frac{C_2}{C_1}
=
\sum_iq_i+q_j.
$$

这要求每个 \(q_j\) 相等。

反方向，当全部 \(q_i\) 相等时，采用更新：

$$
\boxed{
\Pr(B_{t+1}=b+e_i\mid B_t=b)
=
\frac{b_i+1}{t+k}
}
\tag{6}
$$

即可实现每个时间层上均匀的计数分布，以及给定计数后的均匀字串分布。直接连乘即可验证。证毕。

这类“给定终点后路径等权、不同层之间还必须相容”的问题，与分级图上的中心测度研究相衔接；不能把不同层各自合法的概率任意拼接。([arXiv][1])

