## 定义一：\(d\) 周期读数

取：

$$
d\ge2,
\qquad
\omega_d=e^{2\pi i/d}.
$$

定义：

$$
\boxed{
\varepsilon_d(n)
=
\frac{Q_{\mathbf a(n)}(\omega_d)}
{h(\mathbf a(n))}.
}
\tag{2}
$$

它是：

$$
\varepsilon_d(n)
=
\mathbb E_{\gamma}\!\left[\omega_d^{A(\gamma)}\right].
$$

所以：

$$
|\varepsilon_d(n)|\le1.
$$

它可以由一个有限相位寄存器实现：每次增加事件时，将新产生的逆序数累加到：

$$
A\bmod d,
$$

最后读取该寄存器上的相位。

**这个寄存器记录的是历史面积模 \(d\)，不是把实际经过的时间简单改成模 \(d\)。**

项目的 `WindowRegister.lean` 已证明有限周期时钟、移位、Weyl 关系与酉性；但将某个历史统计量装入时钟，仍需要指定实际更新规则。

---

