# 十、这些周期切面不是越来越偏离 ζ：当周期增大时，它们有控制地恢复 ζ

定义全部周期族：

$$
\boxed{
\mathcal H_d(s)
=
\sum_{n\ge1}\frac{\varepsilon_d(n)}{n^s},
\qquad \Re s>1.
}
\tag{24}
$$

对每个固定 \(n\)：

$$
\omega_d\to1
$$

使：

$$
\varepsilon_d(n)\to1.
$$

因此：

$$
\boxed{
\mathcal H_d(s)\longrightarrow\zeta(s)
\qquad(d\to\infty,\ \Re s>1).
}
\tag{25}
$$

而且可以给出速率。

对固定指数向量，最大逆序面积是：

$$
A_{\max}(\mathbf a)=\sum_{p<q}a_pa_q.
$$

历史反转将：

$$
A\longmapsto A_{\max}-A,
$$

所以等权平均满足：

$$
\mathbb E[A]=\frac12A_{\max}.
$$

由：

$$
|e^{ix}-1|\le|x|,
$$

得到：

$$
\begin{aligned}
|\varepsilon_d(n)-1|
&\le
\frac{2\pi}{d}\mathbb E[A]\\
&=
\frac{\pi A_{\max}}d\\
&\le
\frac{\pi\Omega(n)^2}{2d}.
\end{aligned}
$$

又因为：

$$
\Omega(n)\le\frac{\log n}{\log2},
$$

最终：

$$
\boxed{
|\mathcal H_d(s)-\zeta(s)|
\le
\frac{\pi}
{2d(\log2)^2}\,
\zeta''(\sigma),
\qquad \sigma=\Re s>1.
}
\tag{26}
$$

因此，这些周期切面确实属于同一个有界历史母函数，并在原始收敛域中恢复真实 ζ。

但式（26）也直接暴露了新的边界：

$$
\sigma\downarrow1
$$

时，右边不再提供统一控制。

**不能把 \(\Re s>1\) 中的良好收敛，未经证明地带过极点和临界带，再任意交换“周期趋于无穷”与“绕零点解析延拓”的顺序。**

有限周期切面可以有对数分支；它们的极限却可以恢复单值的 ζ。要证明这种变化怎样发生，必须控制整个周期族在目标区域的解析行为，不能只依据初始域中的逐点极限。

---

