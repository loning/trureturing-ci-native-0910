## 核心分解

当 \(b=0\) 时：

$$
f_{d,0}(z)
=
\sum_{r=0}^{d-1}\frac{r!}{[r]_{\omega_d}!}z^r.
$$

它的常数项和一次项分别为 1、1，所以存在只依赖 \(d\) 的常数 \(\lambda_{d,j}\)，使：

$$
\boxed{
\log f_{d,0}(z)
=
z+\sum_{j=2}^{d-1}\lambda_{d,j}z^j
\pmod{z^d}.
}
\tag{30}
$$

于是：

$$
\boxed{
\begin{aligned}
\prod_p f_{d,b_p}(tp^{-s})
={}&
\exp\!\left[
tP(s)+
\sum_{j=2}^{d-1}\lambda_{d,j}t^jP(js)
\right]\\
&\times
\prod_{p\mid m}
\frac{f_{d,b_p}(tp^{-s})}
{f_{d,0}(tp^{-s})}
\pmod{t^d}.
\end{aligned}
}
\tag{31}
$$

最后的商只作为**常数项为一的形式幂级数商**使用。这里没有假定这些有限多项式在整个复平面无零。

关键是：

$$
\exp(tP(s))
$$

提供全部 \(P(s)\) 的幂；其余部分只涉及：

$$
P(2s),\ldots,P((d-1)s)
$$

以及整除 \(m\) 的有限个素数。

因此：

$$
\boxed{
\mathcal H_d(s)
=
\sum_{j=0}^{d-1}C_{d,j}(s)P(s)^j.
}
\tag{32}
$$

在：

$$
\Re s>\frac12
$$

中，全部 \(P(js)\)、\(j\ge2\)，都处在各自绝对收敛域内。

再使用：

$$
P(s)=\log\zeta(s)+R(s),
$$

其中 \(R\) 在 \(\Re s>1/2\) 全纯，得到：

