## 16.1 直接取 \(s=1\)，会发生什么？

每个单独的素数方向仍然可以定义：

$$
C_{p,k}(\tau)
$$

且局部发生率有限。

但仅考虑 \(k=1\)：

$$
\sum_p\frac1p=\infty.
$$

对任意 \(\delta>0\)，事件：

$$
C_{p,1}(\delta)>0
$$

的概率和发散，并且不同素数方向独立。

所以几乎必然有无穷多个素数方向在 \((0,\delta]\) 内发生事件。对全部正有理 \(\delta\) 同时应用这一结论，得到：

$$
\boxed{
\text{每个 }(0,\delta]\text{ 都包含无穷多个事件，}
\quad
\text{不存在第一个正时刻事件。}
}
\tag{49}
$$

这不是“事件不存在”，而是：

$$
\boxed{
\text{不能再用有限速率、从第一步开始逐项枚举的全局时钟描述它。}
}
$$

仓库的 `ZetaPrimeProductCommonBoundary` 已将素数激活可和性、全局有限支撑、熵与参数敏感性的边界统一到 \(s=1\)。这里进一步明确了相应的一种时间表现。

