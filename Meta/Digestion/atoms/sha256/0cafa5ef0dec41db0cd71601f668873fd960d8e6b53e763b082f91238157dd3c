### 证明

取一次多项式 \(f(X)=X\)。反复绕行：

$$
\mathsf T_\delta^kX=X+k\delta.
$$

对于任意正定内积诱导的范数：

$$
\boxed{
\|\mathsf T_\delta^kX\|
\ge
k|\delta|\,\|1\|-\|X\|
\longrightarrow\infty.
}
\tag{54}
$$

若 \(\mathsf T_\delta\) 酉，范数应始终等于 \(\|X\|\)，矛盾。证毕。

所以：

> **有限多项式分支空间中的“绕行一步”，不是有限周期量子时钟的一次无损旋转。**

这与项目的 `WindowRegister` 并不冲突。该模块证明的是有限时钟的根单位谱、酉性和周期闭合；本轮的解析绕行则保留一个可以无界增长的整数记录。

