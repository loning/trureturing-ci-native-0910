## 更强的是：非零信号具有统一门槛

由式（13）：

$$
\boxed{
\nu_{\mathcal C}\ge1
\Longrightarrow
\left|
\Delta_{\mathcal C}^{\,2}\mathcal H_3(s_0)
\right|
>
\frac{8\pi^2}{5}.
}
\tag{21}
$$

所以，只要能够严格认证：

$$
\boxed{
\left|
\Delta_{\mathcal C}^{\,2}\mathcal H_3(s_0)
\right|
<
\frac{8\pi^2}{5},
}
\tag{22}
$$

就可以推出该矩形内没有零点。

这里的优势是：不是仅判断一个可能极小的浮点数是否等于零，而是利用整数计数产生的间隙。

但剩余的难点也很具体：

> **必须认证三份值确实来自同一个函数芽，沿同一条闭路连续运输而来，并严格控制全过程的误差。**

若每次回到起点，都重新调用原来的收敛级数，只取原始分支，就会主动将所有绕行记录重置。这样计算出的差分当然为零，但那不是无零证明。

项目的 `RoucheZeroCount` 已保留严格边界估计、同伦非消失及带重数的零点计数稳定性；它不自动提供这里每一段解析延拓的数值证书。

---

