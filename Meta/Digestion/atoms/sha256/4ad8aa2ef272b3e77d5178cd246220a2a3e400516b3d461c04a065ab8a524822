# 十四、5040在这个时钟下，出现了新的精确时间结构

仍然取：

$$
5040=2^4\,3^2\,5\,7.
$$

在普通单素数时钟下，到达这个终点需要：

$$
4+2+1+1=8
$$

步。

但在素数幂包时钟下，一次 \(2^4\) 跳跃可以完成四层；也可以由 \(2^2\)、\(2\)、\(2\) 等不同事件组合完成。

令：

$$
C(1)=\sum_{p,k}C_{p,k}(1)
$$

表示单位时间中的包事件总数。

