# 十、这次可以怎样推进形式化与实际估计？

当前最有价值的不是再定义一个“正核”，而是把三个不同量分别保留：

$$
\boxed{
A=-\partial_af_a(\theta_*)>0
}
$$

是径向越界速度；

$$
\boxed{
2m=\text{角向接触阶数}
}
$$

是局部形状；

$$
\boxed{
N_{\mathrm{det}}(\delta)
}
$$

是有限历史分辨成本。

它们之间现在有了可证明的连接：

$$
\boxed{
f\approx-A\delta+Bt^{2m}
\Longrightarrow
\text{负区宽度}\asymp\delta^{1/(2m)}
\Longrightarrow
N_{\mathrm{det}}\asymp\delta^{-1/(2m)}.
}
\tag{31}
$$

在数值认证上，还必须把输入误差与信号尺度一起计入。

若 \(T_N\) 的每个矩阵元误差不超过 \(\epsilon_N\)，则

$$
\|\widetilde T_N-T_N\|_{\mathrm{op}}
\le(N+1)\epsilon_N.
$$

若要直接分辨

$$
\lambda_{\min}\asymp(N+1)^{-2m},
$$

一个充分的误差要求是

$$
\boxed{
\epsilon_N=o\!\left((N+1)^{-(2m+1)}\right).
}
\tag{32}
$$

这只是当前逐项矩阵认证路线的充分条件，不是所有算法的精度下界。

本轮的精确接触恒等式、临界行列式与有限谱复核已整理为[核验脚本](sandbox:/mnt/data/critical_contact_order_verify.py)，结果见[核验记录](sandbox:/mnt/data/critical_contact_order_verify.json)。脚本中的模型不是 ξ；没有计算实际 \(a_*\)，也没有把浮点谱值当成严格符号证书。

