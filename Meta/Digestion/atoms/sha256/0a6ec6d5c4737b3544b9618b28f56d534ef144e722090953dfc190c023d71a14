### 34.5 四个区分反例与概率边界

**例 G1（相同边缘像和个别依赖不能决定共同来源加法）。** 取唯一来源集 $S=\{s\}$、$V_s=\{0,1\}$，$\Gamma=\{0,1\}$ 表示该来源的两个赋值。固定整数目标 $F(s)=s$；在模型一令第二输入 $B(s)=s$，在模型二令 $B(s)=1-s$。两模型中，每个输入的边缘像都是 $\{0,1\}$，充分域族都只有 $\{s\}$，因而各自的唯一最小充分域相同。然而共同世界上的联合像与加法像分别为

$$
\begin{array}{c|c|c}
& (F,B)[\Gamma] & (F+B)[\Gamma]\\ \hline
\text{模型一}&\{(0,0),(1,1)\}&\{0,2\}\\
\text{模型二}&\{(0,1),(1,0)\}&\{1\}.
\end{array}
$$

第二个加法目标已经恒定，第一个仍依赖 $s$。这里只是将反例中的第二输入改记为 $B$，以区别于定义 33 的组合输出 $G$；联合约束造成的差别不能从两份边缘像及个别依赖族恢复。

**例 G2（相同可能值像不能决定充分域）。** 取
$S=\{a,b\}$、$V_a=V_b=\{0,1\}$、完整共同域
$\Gamma=\{0,1\}^{\{a,b\}}$。两个整数目标为 $F(\gamma)=\gamma(a)$、
$B(\gamma)=\gamma(b)$，可能值像同为 $\{0,1\}$，但

$$
\operatorname{Dep}(F)=\{\{a\},\{a,b\}\},
\qquad
\operatorname{Dep}(B)=\{\{b\},\{a,b\}\}.
$$

固定 $a$ 就固定 $F$，而不含 $a$ 的域不能区别只改变 $a$ 的合法世界；对 $b$ 同理。所以二者的最小充分域分别为 $\{a\}$ 与 $\{b\}$。

**例 G3（充分域族相等不能反推核相等）。** 取
$S=\{t\}$、$V_t=\{0,1,2\}$、$\Gamma=\{0,1,2\}$，令联合响应只有一个分量
$J(t)=t$。取在这三个值上总定义的 $f(t)=t\bmod2$，其普通结果依次为 $0,1,0$，标签化输出为
$G(t)=\operatorname{succ}(t\bmod2)$。二者均非恒定，唯一来源 $t$ 又足以决定各响应，故

$$
\operatorname{Dep}(J)=\operatorname{Dep}(G)=\{\{t\}\}.
$$

但是 $G(0)=G(2)$ 而 $J(0)\ne J(2)$，所以核不同，$\widehat f$ 在实际像上不单射。这保留了“同一充分域族”和“同一响应区分能力”的区别。

**例 G4（除法的成功常值不是原域的无条件常值）。** 取
$S=\{t\}$、$V_t=\{0,1\}$、$\Gamma=\{0,1\}$，以同一来源作分子和分母，
$J(t)=(t,t)$。沿用 §6、§10 的非零除数守卫，$t/t$ 的严格标签响应是

$$
G(0)=\bot,\qquad G(1)=\operatorname{succ}(1),\qquad
G[\Gamma]=\{\bot,\operatorname{succ}(1)\},\qquad
\Gamma_f=\{1\}.
$$

因此它在原域仍依赖 $t$；仅报告成功可能值 $\{1\}$ 会漏掉一个实际失败世界。只有显式限制到非空 $\{1\}$ 后，才得到恒定的成功响应。若另取共同域为 $\{0\}$，成功域为空：原域上的标签响应仍有意义，但不能把这个空成功域当作合法非空不确定历史。该例消费既有除法守卫，不另立新的除法定理。

本节只有集合像、函数响应及合法共同世界，没有引入分布、条件概率或期望。不同变量名不推出概率独立；即使合法域恰为完整 Cartesian 积，也不能据此推出一个尚未给定的测度满足因子分解。命题 56–61 的结论不依赖这些概率结构。

<a id="p3b-observation-dependency"></a>

