# 十九、不能同时保持有限微步数量与非退化的宏观尺度

现在可以证明一条明确的尺度取舍。

令：

$$
K_\varepsilon(\tau)
$$

为包事件数量。它服从 Poisson 分布，且：

$$
\boxed{
\mathbb E[K_\varepsilon(\tau)]
=
\tau\ln\zeta(1+\varepsilon).
}
$$

另一方面：

$$
\boxed{
\mathbb E[Y_\varepsilon(\tau)]
=
-\tau\varepsilon
\frac{\zeta'(1+\varepsilon)}{\zeta(1+\varepsilon)}
=
\tau(1+o(1)).
}
\tag{60}
$$

