## 定理17.1：临界缩放得到 Gamma 过程的有限维分布

对任意 \(u\ge0\)：

$$
\boxed{
\mathbb E[e^{-uY_\varepsilon(\tau)}]
=
\left[
\frac{
\zeta(1+\varepsilon(1+u))
}{
\zeta(1+\varepsilon)
}
\right]^\tau
\longrightarrow
(1+u)^{-\tau}.
}
\tag{51}
$$

### 证明

因为：

$$
e^{-uY_\varepsilon(\tau)}
=
N_{1+\varepsilon}(\tau)^{-u\varepsilon},
$$

对终点分布求和，相当于把 Dirichlet 参数增加 \(u\varepsilon\)，得到等式。

再由：

$$
\zeta(1+x)=\frac1x+O(1),
$$

比值趋于：

$$
\frac1{1+u}.
$$

证毕。

右边是：

$$
\operatorname{Gamma}(\text{shape}=\tau,\text{rate}=1)
$$

的 Laplace 变换。([现代随机过程][3])

更重要的是，原过程具有独立增量。对任意有限时间组：

$$
0=\tau_0<\tau_1<\cdots<\tau_m,
$$

各段增量的联合 Laplace 变换是式（51）的乘积，因此：

$$
\boxed{
\bigl(Y_\varepsilon(\tau_1),\ldots,Y_\varepsilon(\tau_m)\bigr)
\Rightarrow
\bigl(\mathcal G_{\tau_1},\ldots,\mathcal G_{\tau_m}\bigr),
}
\tag{52}
$$

其中 \(\mathcal G\) 是标准 Gamma 过程。

它在正时间的密度为：

$$
\boxed{
\Pr(\mathcal G_\tau\in dx)
=
\frac{x^{\tau-1}e^{-x}}{\Gamma(\tau)}\,dx,
\qquad x>0,
}
$$

$$
\boxed{
\mathbb E[\mathcal G_\tau]
=
\operatorname{Var}(\mathcal G_\tau)
=
\tau.
}
\tag{53}
$$

这里 \(\Gamma\) 是 Gamma 函数，不是欧拉常数 \(\gamma\)。

**这给出了一个明确的离散—连续联系：素数幂事件组成的乘法过程，在临界对数尺度上，趋向一个非平凡的随机累计过程。**

但证明范围要准确：式（52）是全部有限时间组的联合分布收敛。本节没有宣称已经获得原过程与极限过程之间的统一全路径误差界，更没有由此推出洛伦兹时空。

