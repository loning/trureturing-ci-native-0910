## 本轮的问题(第 6 轮;由 orchestrator 依第 5 轮判词重写)

上一轮已结算、不得重做的结论**已移入 CHARTER.md**,本文件只留问题。

### Q1(最高优先)—— §19 场模型三条的**落地形状**要经得起判形

上一轮周界图左栏的 1–3 项目前**完全不在仓内**(仓里只有 chain 矩阵侧,没有 §19 的
`(ω,k)` 有理函数侧)。我拟把它们作**一个模块**落地:

1. 精确隐藏场 Schur 响应 `H ≠ 0 → D_eff = A − λ²/H`,`H = Ω_h² + c_h²|k|² − ω²`(承逃逸见证);
2. `c_eff² − c₀² = α/(1+α)·(c_h² − c₀²)`,`α = λ²/Ω_h⁴`,及区间 `min(c₀²,c_h²) ≤ c_eff² ≤ max`;
3. 主符号 `p ω k² = −ω² + c_eff²k²` 与 `p = 0 ↔ ω² = c_eff²k²`。

**问题**:按第 5⁴ 条判形,**第 2、3 项是不是 bind-only?** 若把 `Z`、`c_eff²` 按 (19.3)(19.4)
写成定义,那么第 2 项是否只是 `field_simp` + `ring`、第 3 项是否只是定义展开?

- 若**是**,请直说,并指出:除了第 1 项,这个模块还有没有别的真逃逸内容?没有就说没有——
  我会把 2、3 作**具名伴随结果**落地(第 5⁴ 条允许),而不是假装它们是内容。
- 若**不是**,请点名第 2、3 项各自的逃逸见证是什么(哪一步不是定义展开或规范化改写)。

**另外**:第 1 项的 `hidden_field_solve_eq` 在 Lean 里要处理 `H ≠ 0` 之外的什么退化情形?
卷内 §19 对 `H = 0`(即 `ω² = Ω_h² + c_h²|k|²`,隐藏场共振)是怎么处理的?给出源码位置。

### Q2 —— 右栏里哪一条**最接近**可判定,以及它需要的公理能否写成一条 Lean 假设

右栏五项各缺一条额外公理。**不要**替 τ=0 选目标,但请回答一个纯技术问题:

若把那条公理**写成一个显式的 Lean 假设**(而非试图证明它),
哪一条的下游推论**最短、最可判**?即:`assume <公理> ⊢ <结论>` 这个条件定理里,
哪一条的 `⊢` 部分离已冻结件最近?

请给出该条的:公理的 Lean 形、条件定理的 Lean 形、以及它会消费哪些已冻结的 GID。
**明标这是条件定理**——本仓禁止把难点藏进假设位再宣称解决了问题,
所以这条若要落地,必须如实报成 `assume` 形态,且不得声称推进了那个开放问题。

### Q3 —— 卷内错(换一个作用面)

上一轮的靶向审计未找到,你也明说没做全卷机械比对。本轮**换面**:
把审计对准 **§17(涨落界)与 §19 注 19.1 的接口** —— 那里卷自己说
「hidden gap 可以保持而 visible gap → 0」,而 §17 的涨落界依赖谱隙。
问:§17 的哪些具体陈述在 `visible gap → 0` 时**失效**,而卷里有没有在别处仍按它们成立来用?
找到就给双方原文与行号;找不到就明说找不到。
