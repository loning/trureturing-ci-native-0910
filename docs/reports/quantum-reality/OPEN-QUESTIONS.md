## 本轮的问题(第 8 轮;由 orchestrator 依第 6、7 轮判词重写)

已结算、不得重做的结论**在 CHARTER.md**。本文件只留问题。

第 6、7 轮的合并结算在 `/issues/6298`(第五轮之后那条评论)。三件事已经变了:
§19 场模型**已经进仓**;你第 7 轮点名的那个缺口**我已经补上并三方绿**;
右栏的「下敏感度」一项被你从「缺一条公理」升级成了「一条 universal 公理**不可能**覆盖 L3」。
所以本轮不再问 §19 的落地形状——那一页翻过去了。

### Q1(最高优先)—— 把 (17.5) 从定理 17.3 里抽出来:**最弱假设是什么**

你第 7 轮的判词是:定理 17.3 把 (17.5) 与 (17.6) 放在同一个 `hgap`(`ν_a ≥ ν_* > 0`)之下,
而证明显示 **(17.5) 不需要 `ν_*`** —— 它的右端是 `β⁻¹`,证明只用 `x·coth x ≥ 1`,
而 `coth x ≥ 1` 配 `ν_a ≥ ν_*` 是 (17.6) 才要的。你明说这不是卷内错,是 theorem factoring 可更精确。

**本轮把它做成靶。** 请回答三问,每问都要能直译 Lean:

1. **最弱假设**:(17.5) 成立**确切**需要哪些前提?逐条列出并说明每条用在证明的哪一步。
   特别是:`ν_a > 0` 是否仍必需(还是只要 `ν_a ≥ 0`)?`β > 0` 是否必需?
   模式集有限还是可数、求和是否需要收敛前提?**凡你判为「可以去掉」的假设,给出去掉它之后证明仍走得通的那一步。**
2. **核心不等式的支撑面**:`x·coth x ≥ 1`(等价 `x·cosh x ≥ sinh x`,`x ≥ 0`)
   在 mathlib 里**有没有**现成声明?查 `Real.sinh`/`Real.cosh`/`Real.tanh` 周边,
   并按 CHARTER 的口径报「所查来源未找到」而非「不存在」。
   若没有,给出**你会怎么证**的最短路径(级数比较?`sinh x ≤ x cosh x` 的导数论证?),
   并点名它要用的 mathlib 引理。
3. **逃逸见证**:这条更弱版本的 (17.5) 如果落地,**哪一条中间命题是真内容**?
   按 CHARTER 的交付格式给出它的名字,并说明它**不是**哪条已冻结定理的平凡推论。
   如果你判断整条只是把卷里的证明抄进 Lean、没有任何非平凡中间步,**请直说**——
   那我就按伴随结果落地,不为它单独开席。

### Q2 —— 「下敏感度」这一项还该不该留在右栏

你第 7 轮证明了:`UniformLowerSensitivity κ micro obs := 0 < κ ∧ ∀ n, κ·micro n ≤ obs n`
与 L3 链族**不相容**(链族 `micro ≥ 1` 而 `obs → 0` 指数快),
并特别提醒不得偷偷允许 `κ = κ_n → 0`,否则那不是右栏缺的那条公理。

于是右栏第三项的地位变了。**问题**:

1. 有没有一个**非平凡的弱化**——比 universal 弱、但仍强到能推出「微观 gap 可观测」——
   而它与 L3 链族**相容**?给出它的精确形式。
   典型候选:限定到某个模型子类、允许 `κ` 依赖某个额外结构参数(但**不是**依赖 n 本身)、
   或把结论从「下界」改成「某个积分/平均意义下的非零」。
2. 若你判断**任何**这样的弱化要么与 L3 相容却推不出可观测 gap、要么能推出却仍与 L3 冲突,
   那么右栏第三项应当从「缺一条公理」**移到「已判不可能」**。
   **这个答案是允许的、甚至是本轮我最希望拿到的答案之一**——它替我关掉一条不该派席的线。
   给出你据以判断的那个二分:弱化空间的哪一侧对应哪一种失败。
3. 无论 1 还是 2,请给出这条判词的**精确边界**:它反驳了什么、**没有**反驳什么。
   (对照第 5 轮 `c_eff` no-go 的收窄形式——那次你把过强的说法自己收回去了,这次照做。)

### Q3 —— 预算包络:本线在**不引入新公理**的前提下还有没有第四个靶

CHARTER 的右栏五项里,observer→objective 桥、cross-species principle、Einstein 方程三项
都需要 τ=0 点题(第三档),我不会替他选。第 17 条(涨落界替代估计)由 Q1 接手。
下敏感度由 Q2 结算。

**问题**:把这四项放在一边,在**已冻结件 + 卷内已有陈述**的范围内,
还有没有第四个**可派席的靶**——即一条既非 bind-only、又不需要新公理输入的可形式化陈述?

- **有**,就点名它:陈述、逃逸见证、它消费哪些冻结 GID、以及为什么此前七轮没被点到。
- **没有**,就直接写「本线在无新公理输入下已到边际」并说明理由。
  **这是合法终态,不是失败**——依第 2.7 条预算包络,连续无边际改进即触底,正解是换 Γ 而非继续 grind。
  我会据此把本线从「每三小时一轮」降为「等 τ=0 点题再启」,并在 issue 上如实结算。

**不要为了让本轮有产出而在 1 里凑一个靶。** 前七轮你有两次直接答「该停」「已被更强定理覆盖」,
那两次的价值高于任何一个勉强凑出的候选。
