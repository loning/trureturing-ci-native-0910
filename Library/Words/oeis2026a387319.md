---
bibkey: oeis2026a387319
authors: OEIS Foundation Inc.
year: 2025
title: OEIS A387319
doi: null
url: https://oeis.org/A387319
claim: A multiple-of-three residue coincidence characterizes composites except 4, 8, 10, and 25.
strata_touched:
  - D5/S3/Arith/Congruence/MultipleOfThreeCoincidence
license: citation-only
triage: anchor
---

# OEIS A387319

## Provenance and preregistration

Skill context: `lean4`. One Codex implementation worker in the caller's
`consensus-rnd:sshx` implementation attempt; no independent review or model
diversity is claimed here. Fixed source: `e7e63c615460da48effcb4a76f96a3a6730af75d`.
The caller reports independent enumeration at k=1..3000, with zero differences
between the exact predicate below and the proposed classification. The triage
worker reports k=1..5000. These are separately attributed reports, not this
worker's measurements or a kernel proof.

The explicit brief and `oeis2026triage0909.md` (A387319 row and paragraph)
preregister the first-tier OEIS conjecture. The exact target is
`Coincides k ↔ 1 < k ∧ ¬Nat.Prime k ∧ k ∉ ({4,8,10,25} : Finset ℕ)`, where
`Coincides k := ∃ m : ℕ, 6 ≤ m ∧ m ≤ k ∧ 3 ∣ m ∧
3 * ((2*k) % m) = m * ((2*k) % 3)`.
There is no cutoff and no extra hypothesis. No atom was supplied; this brief
is the source obligation.

Before implementing new content, attempt closure from pinned Mathlib and
frozen declarations. If successful, report bind-only and create no module.
Otherwise the proposed escape witness is the divisor/residue criterion:
writing m=3t, the equality is equivalent to t dividing 2k and
`(2k/t) % 3 = (2k) % 3`, with 2≤t and 3t≤k. For 3 not dividing k,
the residue condition is equivalent to t%3=1 (under that divisibility).
This criterion is to be used in the live construction and prime exclusion.
The planned witnesses are t=2 for multiples of 3; t=4 for remaining even
composites; and t=p or 2p for odd composites, with p the least prime factor.
The bound in the last branch requires separating k=25 explicitly.
This is a proposed proof route, not an already verified theorem.

## Verified locator

- URL: https://oeis.org/A387319
- Complete entry opened on 2026-09-09: revision 69, November 10, 2025.
- Machine-readable URL: https://oeis.org/search?q=id:A387319&fmt=json
- Independent HTML source: https://oeis.org/A387319/internal
- The first comment explicitly calls the classification a conjecture.
  Its empirical verification to 10^5 is an OEIS author report.

The entry's separate assertion that even k first coincides at m=12 is
false as written: k=6 coincides at m=6, both fractions being zero.
This ancillary observation is not the selected classification. The
A343311 sequence-equality conjecture is also outside this implementation.

## Initial evidence

The first command after reading the repository instructions was
`make lean-cache-ensure`, exit 0: status=present, mathlib/project=warm,
mathlib_missing_olean_files=0, clonefile_attempts=0, stamp_miss=null,
pin_sha256=sha256:6c4c682ffba051b5744fe7a75ccc99d7f3b20227b3b026f392f3315be0adaa4e.
No cold-tree Lake invocation occurred. Lean is v4.33.0 and Mathlib is pinned
at db584cd6d46c92f209a44c0f1c829460d327499d.

Read the exact signatures of `Nat.not_prime_iff_exists_mul_eq`
(Mathlib/Data/Nat/Prime/Basic.lean:85), `Nat.minFac_sq_le_self`
(Mathlib/Data/Nat/Prime/Defs.lean:368), and `Nat.Prime.dvd_mul`
(same file:422). These supply factors, a bound and prime divisibility,
but do not state a residue coincidence characterization.
Frozen `D5/S3/Arith/EuclidLemma` is the direct Mathlib wrapper;
`D5/S3/Arith/PrimeFactorization` gives a list of prime factors.
Neither identifies the residue predicate.

`git ls-tree origin/dev --name-only D5/S3/` confirms the registered Arith
domain. The existing Congruence subdirectory has 18 Lean files and its
Blueprint mirror has 36 files at the fixed baseline; adding one Lean and
two Blueprint files fits SL-003's 48-file limit. The Arith Blueprint root
already has 48 files and is not used. No new domain or subdirectory is needed.

## Unclaimed

No exhaustive search, universal absence of published proofs, mathematical
novelty, priority, independent review, or larger-conjecture consequence is
claimed. The triage worker's direct-reference audit and enumeration, and the
caller's enumeration, remain attributed reports except where independently
checked below. No downloaded b-file is used as a proof. Results of the Lean
probe, supplementary literature search and repository gates will be recorded
as they occur.

## Bind-only and supplementary literature audit

The clean bind-only probe (`bind-only-clean.log`, exit 1) imports Mathlib and
the two frozen arithmetic wrappers, then tries `exact?` and definitional
simplification. It leaves the complete existential/classification equivalence
unsolved. The earlier probe also contained a nonexistent diagnostic name
`Nat.dvd_prime_mul`; the clean rerun removes it, so the residual goal is not
attributed to that lookup error. This is an unsuccessful concrete attempt,
not a proof that all possible bindings fail. The inspected scope contains no
matching characterization; new divisor and bound arguments are necessary in
the proposed route. Candidate discovery used D5 and pinned Mathlib Nat/number
theory keyword searches, followed by reading the named declarations. Public
GitHub Lean code searches for `A387319`, `"coincident" "mod"`, and
`"coincidence" "fraction"` (limit 30 each) returned zero paths. No third-party
dependency was added and search exhaustiveness is not claimed.

Independently reread the complete primary entry and the relevant fields of
A343311 (rev. 11), A387426 (rev. 107), and A389863 (rev. 29). All three still
label their related claims conjectures. Their new direct references beyond
the triage set are A343270 (rev. 5) and A100484 (rev. 137); both complete JSON
entries were opened. A343270 gives a finite sum for three-part partition
counts, not the residue classification. A100484 describes doubled primes.
Its linked MathWorld Semiprime article concerns products of two primes and
their counting/function identities, not equality of the present fractions.
Lanterman's linked paper, arXiv:1210.2991, was downloaded (14 pages): the
inspected definitions and Theorems 1-11 classify irreducibles under congruent
factorization relations, with units ±1 allowed. This is a different predicate;
no identification with the positive bounded divisor criterion is supplied in
those passages. The Anderson--Frazier DOI landing page returned an Incapsula
block, so that paper is not claimed read. No proof of the selected target
was found in the inspected passages. This scoped finding permits proceeding
under the caller's proof-versus-conjectural-restatement rule.

Network limitations: Python urllib initially received four HTTP 403s; curl
retrieved the entries successfully. Google returned a challenge, and Bing's
exact-A-number query returned an irrelevant result; neither is evidence of
absence. The first HTML text extractor mishandled escaped inequalities;
it was replaced with HTMLParser and the source was reread against full JSON.
PDF extraction required a temporary pypdf install and emitted font-encoding
warnings; only the legible passages above are claimed inspected. No
exhaustive recursive traversal of the doubled-prime reference graph is claimed.

The first Lean fragment compiles with zero diagnostics (`fragment-1.log`,
exit 0): the exact divisor/quotient criterion and its modulo-one construction
helper are proved. The proof uses the positive factor t explicitly, so no
zero denominator or division-by-zero case enters the criterion. The main
classification is not yet claimed at this checkpoint.

The complete classification now compiles with zero diagnostics
(`fragment-2b.log`, exit 0). The first full fragment had two unhelpful
multiplication/division simplifications and multi-location normalization
errors after a contradiction already closed the goal; removing the redundant
simplifications and normalizing the whole context repaired these tactic
errors. The mathematical statement and route were unchanged. The public
result quantifies over every natural k, including 0 and 1. No sorry, custom
axiom, strengthened hypothesis, or finite cutoff was introduced.

Independent worker Python enumeration at k=0..5000 found zero classification
mismatches and zero divisor-criterion mismatches for all 2≤t≤k/3. This uses
an independently generated prime sieve and direct modular arithmetic; exact
Fraction examples were also calculated. At k=14, m=12 gives 4/12=1/3,
matching m=3. At prime k=13, the eligible fractions at m=6,9,12 are
1/3, 8/9, 1/6, all different from 2/3 at m=3. At composite k=25 the eligible
fractions are 1/3, 5/9, 1/6, 1/3, 7/9, 8/21, 1/12, all different from 2/3.
At k=6, both fractions at 3 and 6 equal 0; this genuine zero-valued
coincidence must be included. k=0,1 give false on both sides.
The finite checks are evidence only, not frozen numerical results.
