---
bibkey: oeis2026a388711
authors: OEIS Foundation Inc.
year: 2025
title: OEIS A388711
doi: null
url: https://oeis.org/A388711
claim: Gus Wiseman conjectures that partitions with choosable initial intervals are counted by superdiagonal reversed partitions with the same sum and number of parts.
strata_touched:
  - D5/S1/Words/Compositions/ChoosableInitialIntervals
license: citation-only
triage: anchor
---

# OEIS A388711

The task is a first-tier recent OEIS comment conjecture, with source baseline
`d527e3081957dbd440ca634d821e98b868abf534`. The supplied source reads:

> Conjecture: Also the number of superdiagonal reversed partitions of n into k parts,
> where a sequence (y_1, ..., y_k) is superdiagonal iff y_i >= i for all i = 1..k.

> We say that a set or sequence of nonempty sets is choosable iff it is possible to choose a
> different element from each set. For example, ({1,2},{1},{1,3}) is choosable because we have
> the choice (2,1,3), but ({1},{1,2},{2}) is not.

The initial interval attached to a part x is {1,...,x}. Increasing order is
the reversed-partition convention. Indices in Lean start at zero, so the
superdiagonal bound must be i+1. Positive representatives are required even
when the list contains zero.

## Provenance and preregistration

Skill context: `lean4`; implementation by this Codex worker in the supplied
worktree, with no independent review seat at this checkpoint. The caller's
enumeration and literature search are reported below as caller evidence.
No atom identifier was supplied: the explicit OEIS implementation brief is
the source obligation, so `show-atom` is not applicable to this task.

The caller preregistered the sorted-list SDR equivalence as the proposed
escape witness for the counting identity. Its necessary direction restricts
an injective representative map to the first i+1 positions and proves that
its image lies in {1,...,y_i}; cardinal comparison forces i+1 <= y_i.
Its sufficient direction constructs the representatives i+1. Bind-only
inspection must finish before a Lean module is created.

## Search log

- 2026-09-09, worker: `git rev-parse HEAD` returned the exact baseline above.
  Read CLAUDE.md, both agent brief notes, agents/CONTEXT.md, and the complete
  existing Compositions module FirstSumsPartitionCharacterization.
- Worker: `rg -n -i 'superdiagon|initial interval|distinct representative|choosable|A388711' D5 -g '*.lean'`
  returned 5 matching lines: Tribonacci Names, two Sumfree comments, a Jacobi
  matrix comment, and GoldenResource layers. None states the target SDR
  characterization. This is a bounded textual search, not a coverage proof.
- Positive control: `rg -n '\bfirstSums\b' D5/S1/Words/Compositions -g '*.lean'`
  returned 10 matching lines. Both commands use rg alternation/boundary support;
  no unsupported `git grep -E` word-boundary syntax was used.
- Environment discrepancy: `.lake` is absent in the supplied worktree at the
  first inspection, despite the brief's prior ensure report. The worker will
  use `make lean-cache-ensure` before any Lean compilation.
- Caller report, 2026-09-09: four web keyword groups including
  "initial intervals SDR sorted a_i >= i" found no published statement of
  this partition-count identity. General Hall theory and SDRs are classical.
  The underlying initial-interval criterion may be a familiar folklore
  consequence of Hall; it must not be presented as a new mathematical theorem.
- Caller report: exhaustive backtracking for choosability and direct checks
  of superdiagonality agreed cell by cell for n=1,...,13, also agreeing with
  OEIS DATA after aligning its k=0 and n=0 entries.
- Worker directly fetched `https://oeis.org/search?q=id:A388711&fmt=json`:
  the name, choosability definition, initial-interval definition and conjecture
  match the brief. The response has revision 7, author date September 23,
  2025 and creation time September 25, 2025. Thus the brief's 2026 attribution
  differs from the opened source; the requested bibkey is retained, but the
  bibliographic year is corrected to 2025. The first-tier classification
  (2024-2026) is unaffected.
- Preliminary public Lean index check, using authenticated `gh search code`
  with `language:Lean`, limit 20: `"superdiagonal"` returned 2 matrix-related
  paths; `"choosable"` returned 5 distributed-protocol paths. These are
  candidate-discovery results, not semantic coverage conclusions. Ordered
  Mathlib inspection and the subsequent ecosystem check remain pending.

## Limits

Ordered library inspection, worker, 2026-09-09: Mathlib HEAD is
`db584cd6d46c92f209a44c0f1c829460d327499d` (v4.33.0), matching the manifest.
The regex `superdiagon|choosable|initial intervals|distinct representative|parking.function`
over Mathlib returned 3 Hall documentation lines. The Hall theorem inspected
is `Finset.all_card_le_biUnion_card_iff_exists_injective`; it leaves the
arbitrary-subfamily union cardinal inequalities. The general cardinal lemma
is `Fintype.card_le_of_injective`; `List.Pairwise.sortedLE` supplies monotonicity
of list lookup. No frozen D5 theorem is a direct dependency (GID/statement_id:
none). The cache ensure returned exit 0, seeded by clonefile with both layers
warm and no missing Mathlib oleans.

After this Mathlib inspection, public GitHub Lean searches (limit 20) for
`"initial intervals"`, `"superdiagonal" "partition"`, and `"parking" "function"`
returned respectively 5 Abel-summation paths, 0 paths, and 11 protocol or
complexity paths. No matching target theorem was found in this searched scope.
These path-level candidates were not imported; no third-party dependency is
added. The direct Hall-instantiation and normalization probe is now staged
outside the repository at `/tmp/a388711-bind-only.lean`.

The bind-only probe returned exit 1: direct Hall instantiation has right side
`forall s, s.card <= (s.biUnion intervals).card`, not the desired index bound;
definition-only simp leaves the full SDR/index equivalence unsolved. This
failure alone is not an impossibility proof. Together with the inspected
upstream statements, it identifies the missing live construction: restrict
the representative map to a prefix, bound its image, and apply cardinality.
The implementation proceeds with `proof_shape: content` and
`admission_basis: escape-witness`, subject to kernel verification. The
proposed witness remains exactly the prefix argument preregistered by the
caller. The general Hall theorem is not needed in the final module.

First local implementation compile returned exit 1 with three elaboration
errors: implicit Fin bounds in congrArg and two unreduced function applications
in arithmetic goals. The next revision supplies the Fin type and explicit
successor arithmetic. No failing proof is described as kernel verified.

The second compile returned exit 0. The sorted-list equivalence and the
same-sort count equality both have only propext, Classical.choice and
Quot.sound in their printed axiom closures. The next revision adds order
invariance via Mathlib's `List.perm_comp_forall₂`, transporting the nodup list
of representatives, and uses it on the live count path: descending parts on
the left, ascending reversed parts on the right. Named consumer direction:
`card_choosable_eq_superdiagonal -> choosableInitial_congr_perm`.

The first permutation revision exposed one cast-normalization error in
`List.get_ofFn`; adding the upstream `Fin.cast_mk` rewrite addresses that
specific goal. The prefix theorem remained verified in this compile. The
next validation is the full repository Lean entry point with LAKE_JOBS=3.

ASSUMED-UNVERIFIED at this checkpoint: the general local proof and its gates
have not yet run. The caller's literature search is not an exhaustive search and
does not establish that no published, private, or unindexed proof exists.
No first-publication priority and no implication to a larger conjecture are
claimed. Subsequent checkpoints will record the actual Lean and gate results.
