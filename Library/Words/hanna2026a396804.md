---
bibkey: hanna2026a396804
authors: Paul D. Hanna
year: 2026
title: OEIS A396804 — E.g.f. satisfies A(x) = x exp(A^4(x))
doi: null
url: https://oeis.org/A396804
claim: The entry conjectures a(n) congruent to n modulo four for positive n; superscripts denote composition.
strata_touched: [S1]
license: citation-only
triage: anchor
---

# A396804: the modulo-four conjecture

Source checked on 2026-09-09 via the OEIS JSON endpoint. The entry was created
2026-06-09 and the retrieved revision is 23. It explicitly defines
`A = X exp(A∘A∘A∘A)` and `a(n) = n! [x^n] A`, with offset 1.
The modulo-four sentence is still marked **Conjecture**. Its first ten terms
are `1, 2, 27, 820, 41005, 2933046, 277407235, 33008920904,
4775699432745, 819537205428010`.

The old modulo-three pattern is explicitly refuted at n=5 in the entry.
The replacement six-cycle is a separate conjecture, not the target here.
The seven distinct direct A references are A000169, A140054, A396799,
A396800, A396803, A396805, A396806; A000169 occurs twice in the xref field.

## Search and implementation record

The earlier triage is recorded in `oeis2026triage0909`. This implementation
independently reread its A396804 row and paragraph, and the main OEIS JSON.
The triage seat's report of reading all fields of all seven linked entries,
its supporting-text review, and its n≤40 computation remain **attributed
reports**, not repeated independent checks by this worker.

The first bind-only search inspected the frozen ordinary-series theorem
`CompositionalIterateCongruence` and the separate EGF/ODE construction
`ExponentialImplicitParityPeriodThree`. Neither gives the required EGF
composition integrality or this congruence. Pinned Mathlib v4.33.0 declarations
`PowerSeries.coeff_exp`, `PowerSeries.coeff_subst'`,
`PowerSeries.derivative_subst`, and
`PowerSeries.constantCoeff_iterate_derivative` were read. They supply
coefficient and derivative calculus, not the target theorem.

An authenticated GitHub code search for `A396804 language:Lean` returned
`total_count=0`; an arXiv API exact-phrase query for
`iterative exponential generating` returned `totalResults=0`. Google returned
an access interstitial and Bing returned irrelevant flight results, so these
two searches supply **no negative literature evidence**. No proof of the
target has been found within this bounded search.

Before implementation the proposed escape was retained: the EGF coefficients
of `A∘A∘A∘A-X` should be divisible by four. The refinement uses the integer
chain-rule recurrence underlying Bell composition. For `F=X exp(X)`, the
planned square formula is
`n![x^n](F∘F)=Σ_k binomial(n,k) k k^(n-k)`.
Modulo two this reduces to `n*2^(n-1)`. Writing `F∘F=X+2H`, integral composition
congruence should give `F^4=X mod 4`; contraction then transfers this to A.
The three Lean units now compile this route without sorry or added axioms.

The first compiled unit establishes factorial-normalized multiplication and
the chain-rule composition recurrence over arbitrary commutative semirings,
its compatibility with ring maps and finite coefficient prefixes, and the
preservation of natural/integer EGF coefficients under valid substitution.
Its file check exited 0. The baseline `make lean` exited 0 (12797 jobs).
The ensure receipt was `seeded/clonefile`, with `stamp_miss=null`, both layers
warm; the subsequent source/olean set difference was **3 missing of 3961**.

## Implementation evidence

The second unit constructs a : Nat → Nat, proves
`(a n : Rat) = n! * coeff n A`, constantCoeff A = 0, the exact fourfold
equation, and existence and uniqueness over PowerSeries Rat. The third
proves `mod_four` for all n≥1 and `fourth_coeff_divisible` for all n≥2.
All four principal declarations print only `propext`, `Classical.choice`,
and `Quot.sound`. The final full `make lean` exited 0 (12800 jobs), with
no warnings after removing unused convenience declarations and wrapping
long lines. Final report, ledger alignment, emission and PR gates follow.

Lean computes a(0)=0, a(1)=1, a(2)=2, a(3)=27, and both sides of the
nonempty residue example: 27 mod 4 = 3 mod 4 = 3. It also computes the
second EGF coefficient of the fourth iterate as 8, with 8 mod 4 = 0.
These private/anonymous sanity checks are companions to the unbounded
proof, not independently admitted finite instances or escape witnesses.

A separate Fraction/Horner implementation was run through n=34. All first
ten values agree with the caller, all A and B EGF denominators are one,
and both modulo-four checks have zero failures in their stated ranges.
In particular B(2)=8, B(3)=180 and B(4)=7240. These are diagnostic finite
checks, separate from the unbounded Lean proofs.

## Admission account

All three modules have `admission_basis: escape-witness` and `utility: none`
(unbounded symbolic mathematics). The active path is the new integral
composition recurrence and its prefix/reduction lemmas, then the natural
contractive fixed-point construction, then the new fourth-iterate arithmetic
of F=X exp(X). `linear_fourth_mod_four` feeds the approximation invariant
and `mod_four`. The proposed B(A) divisibility is proved as a companion;
it is not used circularly as a premise of the target.

The first module has no D5 imports. The second directly imports the first;
the third directly imports the second and uses both preceding namespaces.
There are no pre-existing frozen D5 theorem dependencies. Exact candidate
module statement identities and the declaration-by-declaration proof-shape
account will be recorded from the canonical report before publication.
The motivating GID in the dossier is context, not a proof dependency.

## ASSUMED-UNVERIFIED

No claim of exhaustive literature or Lean-library search is made. This
Library note is an evidence record; the proof is in the named Lean modules.
The triage seat's seven-page review, supporting-text review and integer Bell
computation to n=40 remain attributed reports; they have not been rerun.
The worker independently read the main JSON and recomputed through n=34.
Not every OEIS revision was inspected. A supplemental GitHub Hurwitz query
returned candidates that were not inspected and supplies no absence claim.

Identification with OEIS uses its retrieved equation and initial terms;
the Lean kernel does not authenticate the external page. No first-proof
priority, independent review or model diversity is claimed. This is one
Codex main thread using the lean4 skill; later runner reviews lie outside
this worker's evidence. No modulo-three conjecture is resolved here.
